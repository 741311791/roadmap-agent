---
description: DeerFlow 上游同步与冲突处理规范
alwaysApply: true
---

# DeerFlow 上游同步与冲突处理规范

## 触发条件
- 仅当任务涉及 `deer-flow` 官方仓库更新、`merge`、`rebase`、冲突处理、依赖修复或升级后的兼容性排查时启用本规则。

## 核心目标
- 持续跟进官方 `upstream/main`，同时保留 `roadmap-agent` 的定制适配。
- 优先吸收官方主干实现，再最小化补回本地适配，禁止直接覆盖整个目录。

## 固定约束
1. 所有同步操作必须在 `deer-flow` 独立仓库内执行，不要在主仓库根目录误操作。
2. 同步前必须先执行 `git status`；若存在未提交改动，优先提交，次选 `git stash`。
3. 官方远程统一命名为 `upstream`，项目自有长期分支统一命名为 `roadmap-agent-main`。
4. 禁止用“复制官方代码覆盖本地目录”的方式更新 `deer-flow`，必须基于 git 合并历史处理。
5. 遇到 `uv.lock`、`package-lock.json` 等 lock 文件冲突时，禁止手工拼接内容；应先合并声明文件，再重新生成 lock。

## 路径说明
- 下文 **`deer-flow/`** 均指主仓库内的子目录 `roadmap-agent/deer-flow`（独立 Git 仓库）。
- **必查点位**中带 `backend/app/...` 的路径属于 **主仓库** `roadmap-agent`；带 `deer-flow/...` 的路径属于 **deer-flow 子仓库**。

## 一次性初始化（仓库中尚无 `upstream` 或 `roadmap-agent-main` 时）
1. **`upstream` 远程**：在 `deer-flow/` 内执行 `git remote add upstream https://github.com/bytedance/deer-flow.git`（已存在则跳过）。说明：`origin` 可能与 `upstream` 同指向官方；仍须保留 `upstream` 命名，以便日后 `origin` 改为 fork 时流程不变。
2. **`roadmap-agent-main` 分支**：在 `deer-flow/` 内从当前承载工作的分支（通常为 `main`）创建并检出：`git checkout main && git branch roadmap-agent-main && git checkout roadmap-agent-main`。此后 **例行同步与 deer-flow 侧定制提交** 默认在 `roadmap-agent-main` 上进行。
3. **`main` 与 `roadmap-agent-main` 的关系（二选一，团队内统一即可）**：
   - **策略 A（推荐）**：日常只在 `roadmap-agent-main` 合并 `upstream/main`；`main` 仅作参照，需要时再与 `roadmap-agent-main` 对齐。
   - **策略 B**：每次同步验证通过后，将 `main` **快进** 至与 `roadmap-agent-main` 同一提交（`git checkout main && git merge --ff-only roadmap-agent-main`），便于脚本或习惯依赖分支名 `main`。

## 标准流程（例行同步）
1. 在 `deer-flow` 仓库中检查工作区状态并保存本地改动（`git status` → 提交或 `git stash`）。
2. 执行 `git fetch upstream` 获取官方最新提交。
3. 切换到 `roadmap-agent-main`。
4. 执行 `git merge upstream/main` 合并官方更新。
5. 若出现冲突，按「冲突处理原则」逐文件解决。
6. **合并后固定动作**：
   - 在 `deer-flow/backend/` 执行 `uv lock`（或项目约定的等价命令）刷新 `uv.lock`；若官方变更涉及前端依赖，在 `deer-flow` 前端目录按官方说明重装依赖并更新 lock。
   - **最小可用验证**：例如 `make check`、启动 gateway、跑一次关键接口或聊天冒烟（以当前项目约定为准）。
   - **主仓库桥接层复查**：对照「必查点位」中主仓库路径，确认 API、配置键名、路径变更未破坏代理与上下文拼装（不得仅在 `deer-flow` 内解决冲突后结束）。
7. 验证通过后，在 `deer-flow` 内提交 **一次独立的同步 commit**（勿与主仓库改动混在同一 commit，除非团队另有约定）。

## 本地配置与示例文件（合并后易漏项）
- 合并官方后若 `config.example.yaml`、`.env.example` 有新增项：将 **必要项** 合并进本地 **`deer-flow/config.yaml`**、**`deer-flow/.env`**（勿提交密钥）；禁止在未理解含义时删除本地已有集成相关配置。

## 冲突处理原则
- 业务源码冲突：先理解官方改动目的，再补回本地集成逻辑；不要默认整文件保留 `ours`。
- 配置文件冲突：将结果视为“第三份手工合并版本”；优先保留官方新增字段，再补回本地配置值。
- 依赖文件冲突：以 `pyproject.toml`、`package.json` 等声明文件为准，然后重新执行 `uv lock`、`npm install` 等命令生成新 lock。
- 如果官方发生目录重构、接口重命名或配置项变更，必须额外排查项目中的 DeerFlow 桥接层，不得只解决文本冲突后结束。

## 必查点位
**主仓库（`roadmap-agent`）**
- `backend/app/services/learning/deerflow_proxy_service.py`
- `backend/app/services/learning/deerflow_context_service.py`
- `backend/app/services/learning/memory_service.py`

**deer-flow 子仓库**
- `config.yaml`、`config.example.yaml`、`.env.example`
- `backend/pyproject.toml`、`backend/uv.lock`

## 输出要求
- 说明本次同步基于哪个官方分支；并记录合并时 **`upstream/main` 的提交 SHA**（例如 `git rev-parse upstream/main`）。
- 明确列出冲突文件、处理原则、依赖重建动作和验证结果。
- 如果发现无法安全自动决策的冲突，必须先停下并向用户说明分歧点，再继续修改。

## 其他信息
- deer-flow官方github地址：https://github.com/bytedance/deer-flow