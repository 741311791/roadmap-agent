# Celery 连接池隔离修复完成

**日期**: 2026-01-13  
**状态**: ✅ 已完成

## 问题根因

### 架构设计问题
路线图生成运行在 **Celery Worker** 中，但搜索工具错误地使用了 **FastAPI 的连接池**，导致：
1. **数据库连接池冲突**：`IllegalStateChangeError`
2. **内容生成数据未保存**：`roadmap_id` 缺失

---

## 关键错误分析

### 1. **连接池混用错误**（终端日志多处）
```python
IllegalStateChangeError: Method 'close()' can't be called here; 
method '_connection_for_bind()' is already in progress
```

**错误链路**：
```
Celery Worker
  → TutorialGeneratorAgent
    → WebSearchRouter.execute()
      → get_db_transaction()  ❌ 错误：应使用 get_celery_session()
        → TavilyAPISearchTool
          → IllegalStateChangeError
```

---

### 2. **TavilyKeyRepository 未定义**
```python
error="name 'TavilyKeyRepository' is not defined"
```

**位置**：`backend/app/tools/search/tavily_api_search.py` 第73行

**原因**：使用了不存在的类，应使用 `TavilyKeyManager`

---

### 3. **roadmap_id 缺失**（终端日志行510）
```
content_handler_missing_roadmap_id task_id=63551bca-2e40-413c-aee1-de51f8bf29fd
```

**位置**：`backend/app/core/orchestrator/nodes/content_generation.py`

**原因**：节点返回值中缺少 `roadmap_id` 字段

---

## 修复方案

### 修复1：WebSearchRouter 环境智能检测

**文件**：`backend/app/tools/search/web_search_router.py`

```python
# ✅ 添加环境检测逻辑
if db_session is None:
    # 检测运行环境：Celery worker 还是 FastAPI
    try:
        from celery import current_task
        is_celery = current_task is not None and hasattr(current_task, 'request')
    except (ImportError, AttributeError):
        is_celery = False
    
    if is_celery:
        # Celery 环境：使用 get_celery_session()
        from app.db.celery_session import get_celery_session
        async with get_celery_session() as session:
            # 在 session 生命周期内完成所有数据库操作
            tavily_available = await self._has_valid_tavily_keys(session)
            if tavily_available:
                # 尝试 Tavily
                tavily_tool = TavilyAPISearchTool(db_session=session)
                result = await tavily_tool.execute(input_data)
                return result
        
        # session 已关闭，回退到 DuckDuckGo（无需数据库）
        if self.duckduckgo_tool:
            return await self.duckduckgo_tool.execute(input_data)
    else:
        # FastAPI 环境：使用 get_db_transaction()
        from app.db.session import get_db_transaction
        async for session in get_db_transaction():
            db_session = session
            break
```

**关键改进**：
- ✅ 检测运行环境（Celery vs FastAPI）
- ✅ Celery 环境使用 `get_celery_session()`
- ✅ FastAPI 环境使用 `get_db_transaction()`
- ✅ 在 async with 内部完成所有数据库操作，避免连接泄漏

---

### 修复2：TavilyAPISearchTool 使用 TavilyKeyManager

**文件**：`backend/app/tools/search/tavily_api_search.py`

```python
# ❌ 修复前
self.repo = TavilyKeyRepository(db_session) if db_session else None

# 后续使用
if self.repo:
    key_record = await self.repo.get_best_key()

# ✅ 修复后
self._db_session = db_session

# 后续使用
if self._db_session:
    from app.core.tavily_key_manager import TavilyKeyManager
    from app.models.database import TavilyAPIKey
    
    manager = TavilyKeyManager(TavilyAPIKey)
    key_record = await manager.get_best_key(self._db_session)
```

---

### 修复3：content_generation_node 添加 roadmap_id

**文件**：`backend/app/core/orchestrator/nodes/content_generation.py`

```python
# ✅ 返回值中添加 roadmap_id
return {
    "roadmap_id": roadmap_id,  # 新增字段
    "tutorial_refs": {...},
    "resource_refs": {...},
    "quiz_refs": {...},
    "failed_concepts": [...],
    "current_step": "content_generation",
    "execution_history": [...]
}
```

---

## 架构说明

### Celery vs FastAPI 连接池隔离

| 环境 | 连接池类型 | Session 工厂 | 使用场景 |
|------|-----------|------------|---------|
| **FastAPI** | QueuePool（连接池） | `get_db_transaction()` | API 请求处理 |
| **Celery** | NullPool（无池化） | `get_celery_session()` | 异步任务 |

**为什么需要隔离**：
- Celery 使用 `prefork` 模式，子进程会继承父进程的连接池引用
- FastAPI 的 QueuePool 在跨进程共享时会导致连接状态混乱
- NullPool 每次创建新连接，避免跨进程状态共享

---

## 修复效果

### 修复前
- ❌ Celery 环境错误使用 FastAPI 连接池
- ❌ `IllegalStateChangeError` 频繁出现
- ❌ 内容生成数据无法保存到数据库
- ❌ `TavilyKeyRepository` 未定义错误

### 修复后
- ✅ 环境智能检测，使用正确的连接池
- ✅ 无连接池冲突错误
- ✅ 内容生成数据正常保存
- ✅ Tavily 搜索工具正常工作

---

## 验证清单

- [ ] 重新运行路线图生成任务
- [ ] 确认无 `IllegalStateChangeError` 错误
- [ ] 确认数据库 `tutorials` 表有新记录
- [ ] 确认数据库连接池不再混用
- [ ] 验证 Tavily 搜索功能正常（在 Celery 中）

---

## 相关文件

**修改文件**：
1. `backend/app/tools/search/web_search_router.py` - 环境检测和 session 隔离
2. `backend/app/tools/search/tavily_api_search.py` - 修复 TavilyKeyRepository 引用
3. `backend/app/core/orchestrator/nodes/content_generation.py` - 添加 roadmap_id
4. `backend/app/tools/search/duckduckgo_search.py` - 添加查询验证
5. `backend/app/agents/tutorial_generator.py` - 添加工具参数验证

**相关设计文档**：
- `backend/app/db/celery_session.py` - Celery 专用会话管理
- `backend/app/db/session.py` - FastAPI 专用会话管理

