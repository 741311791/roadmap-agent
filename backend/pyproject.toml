[project]
name = "learning-roadmap-backend"
version = "1.0.0"
description = "个性化学习路线图生成系统后端"
authors = [
    {name = "Learning Roadmap Team"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlmodel>=0.0.22",
    "sqlalchemy-crud-plus>=1.13.0",
    "asyncpg>=0.30.0",
    "redis[hiredis]>=5.2.0",
    "langgraph>=1.0.2",
    "litellm>=1.81.1",
    "instructor>=1.0.0",
    "aiohttp>=3.9.2",
    "httpx>=0.28.1",
    "aioboto3>=13.3.0",
    "structlog>=24.4.0",
    "tenacity>=9.0.0",
    "jinja2>=3.1.4",
    "msgspec>=0.18.0",
    "opentelemetry-api>=1.28.2",
    "opentelemetry-sdk>=1.28.2",
    "opentelemetry-instrumentation-fastapi>=0.49b2",
    "alembic>=1.13.0",
    "greenlet>=3.0.0",
    "psycopg2-binary>=2.9.9",
    "psycopg[binary]>=3.1.0",
    "langgraph-checkpoint-postgres>=1.0.0",
    "psycopg-pool>=3.2.0",
    "httpx-ws>=0.8.2",
    "openai>=2.8.1",
    "ddgs>=9.9.3",
    "tavily-python>=0.5.0",
    # MCP (Model Context Protocol) 支持
    "mcp>=1.0.0", # MCP Python SDK
    # FastAPI Users 认证相关
    "fastapi-users[sqlalchemy]>=13.0.0",
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "resend>=2.0.0",
    # Celery 异步任务队列
    "celery>=5.4.0",
    "celery-pool-asyncio>=0.2.0", # ✅ 稳定版本，生产环境验证
    "kombu>=5.4.0",
    "flower>=2.0.0",
    # 定时任务调度
    "schedule>=1.2.0",
    # API 可视化工具
    "fastapi-voyager>=0.14.0",
    "langchain>=1.2.6",
    "langchain-litellm>=0.3.0",
    "langchain-mcp-adapters>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio>=0.24.0",
    "polyfactory>=2.17.0",
    "black>=24.10.0",
    "ruff>=0.8.3",
    "mypy>=1.13.0",
]

# uv 依赖组配置（uv 使用 dependency-groups 而不是 optional-dependencies）
[dependency-groups]
dev = [
    "pytest>=8.3.4",
    "pytest-asyncio>=0.24.0",
    "polyfactory>=2.17.0",
    "black>=24.10.0",
    "ruff>=0.8.3",
    "mypy>=1.13.0",
    "rich>=13.7.0",
]

# Poetry 配置（可选，用于兼容 Poetry）
[tool.poetry]
name = "learning-roadmap-backend"
version = "1.0.0"
description = "个性化学习路线图生成系统后端"
authors = ["Learning Roadmap Team"]
readme = "README.md"
packages = [{include = "app"}]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.115.0"
uvicorn = {extras = ["standard"], version = "^0.32.0"}
pydantic = "^2.9"
pydantic-settings = "^2.6"
sqlmodel = "^0.0.22"
asyncpg = "^0.30.0"
redis = {extras = ["hiredis"], version = "^5.2.0"}
langgraph = "^1.0.2"
litellm = "^1.81.1"
aiohttp = "^3.9.2"
httpx = "^0.28.1"
aioboto3 = "^13.3.0"
structlog = "^24.4.0"
tenacity = "^9.0.0"
jinja2 = "^3.1.4"
msgspec = "^0.18.0"
ddgs = "^1.0.0"
opentelemetry-api = "^1.28.2"
opentelemetry-sdk = "^1.28.2"
opentelemetry-instrumentation-fastapi = "^0.49b2"
alembic = "^1.13.0"
greenlet = "^3.0.0"
psycopg2-binary = "^2.9.9"
psycopg = {extras = ["binary"], version = "^3.1.0"}
langgraph-checkpoint-postgres = "^1.0.0"
psycopg-pool = "^3.2.0"
tavily-python = "^0.5.0"
mcp = "^1.0.0"  # MCP (Model Context Protocol) 支持
schedule = "^1.2.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.3.4"
pytest-asyncio = "^0.24.0"
polyfactory = "^2.17.0"
black = "^24.10.0"
ruff = "^0.8.3"
mypy = "^1.13.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.black]
line-length = 100
target-version = ['py312']

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true

[tool.uv]
# 默认安装开发依赖组
default-groups = ["dev"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# 修复事件循环冲突：为每个测试函数创建独立的事件循环
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
markers = [
    "integration: 集成测试",
    "e2e: 端到端测试",
    "real: 真实E2E测试（需要真实服务运行）",
    "slow: 慢速测试（包含完整工作流和教程生成）",
    "benchmark: 性能基准测试",
]

