# 阶段2：核心模块重构计划

> **优先级**: P1-High  
> **工期**: 2周（10个工作日）  
> **目标**: 重构最复杂的核心模块，建立可复用的重构范式

---

## 📋 执行摘要

### 核心目标

重构3个最复杂的核心模块，建立标准化的重构模板：

1. **generation.py** - 路线图生成核心（1023行→200行）
2. **modification.py** - 路线图修改逻辑
3. **重试逻辑统一** - 提取RetryService，消除重复代码

### 关键成果

- ✅ **代码量降低80%**：generation.py从1023行降至200行
- ✅ **建立重构范式**：提供可复用的迁移模板
- ✅ **测试覆盖率提升**：从<40%提升至>80%
- ✅ **职责清晰**：API/Service/CRUD三层严格分离

### 工期分配

**Week 1**:
- Day 1-2: generation.py Schemas提取 + CRUD层创建（12小时）
- Day 3-4: generation.py Service层提取（12小时）
- Day 5: generation.py API层重构 + 测试（6小时）

**Week 2**:
- Day 6-7: modification.py重构（应用范式）（10小时）
- Day 8: 重试逻辑统一（RetryService）（6小时）
- Day 9: 集成测试 + 性能测试（6小时）
- Day 10: 文档更新 + Code Review（4小时）

---

## 🎯 背景与动机

### generation.py现状分析

#### 代码规模
- **总行数**: 1023行
- **辅助函数**: 3个大型函数（共400+行）
- **endpoint数量**: 6个
- **复杂度**: 圈复杂度>15（极高）

#### 主要问题

**问题1: API层包含大量业务逻辑**

```python
# app/api/v1/endpoints/generation.py (Line 457-657)
@router.post("/{roadmap_id}/concepts/{concept_id}/tutorial/retry")
async def retry_tutorial(...):
    """重试教程生成 - 200行函数"""
    
    # ❌ 业务逻辑全在API层
    # 1. 创建任务记录 (30行)
    async with repo_factory.create_session() as session:
        task_repo = repo_factory.create_task_repo(session)
        await task_repo.create_task(...)
    
    # 2. 获取概念 (40行)
    concept, context, roadmap_metadata = await _get_concept_from_roadmap(...)
    
    # 3. 更新状态为generating (20行)
    await _update_concept_status_in_framework(...)
    
    # 4. 发送WebSocket通知 (15行)
    await notification_service.publish_concept_start(...)
    
    # 5. 调用Agent生成 (20行)
    tutorial_generator = TutorialGeneratorAgent()
    result = await tutorial_generator.execute(...)
    
    # 6. 更新状态为completed (25行)
    await _update_concept_status_in_framework(...)
    
    # 7. 保存教程元数据 (20行)
    async with repo_factory.create_session() as session:
        tutorial_repo = repo_factory.create_tutorial_repo(session)
        await tutorial_repo.save_tutorial(...)
    
    # 8. 发送完成通知 (15行)
    await notification_service.publish_concept_complete(...)
    
    # 9. 更新任务状态 (15行)
    async with repo_factory.create_session() as session:
        task_repo = repo_factory.create_task_repo(session)
        await task_repo.update_task_status(...)
```

**问题2: 辅助函数职责不清**

```python
# Line 291-330
async def _get_concept_from_roadmap(...) -> tuple:
    """40行辅助函数，应该在Service层"""
    # ❌ 包含复杂的业务逻辑
    async with repo_factory.create_session() as session:
        roadmap_repo = repo_factory.create_roadmap_meta_repo(session)
        roadmap_metadata = await roadmap_repo.get_by_roadmap_id(roadmap_id)
    
    if not roadmap_metadata:
        return None, None, None
    
    # 遍历查找概念
    for stage in framework_data.get("stages", []):
        for module in stage.get("modules", []):
            for concept in module.get("concepts", []):
                if concept["id"] == concept_id:
                    return concept, ..., roadmap_metadata
```

**问题3: 重复代码严重**

- `retry_tutorial()`: 200行
- `retry_resources()`: 180行（90%重复）
- `retry_quiz()`: 180行（90%重复）

三个函数只有Agent类型不同，逻辑几乎完全一致。

---

## ✅ 详细任务清单

### 任务1: generation.py Schemas提取 [Day 1-2]

#### 创建完整的Schemas

```python
# app/schemas/roadmap.py (新增)
"""
路线图生成相关Schemas
"""
from pydantic import BaseModel, Field
from typing import Optional
from datetime import datetime

from app.models.domain import LearningPreferences

# ===== 生成请求 =====

class RoadmapGenerateRequest(BaseModel):
    """路线图生成请求"""
    user_id: str = Field(..., description="用户ID")
    preferences: LearningPreferences = Field(..., description="学习偏好")
    
    model_config = {"json_schema_extra": {
        "example": {
            "user_id": "user-123",
            "preferences": {
                "learning_goal": "成为全栈开发工程师",
                "current_level": "beginner",
            }
        }
    }}

class RoadmapGenerateResponse(BaseModel):
    """路线图生成响应"""
    task_id: str = Field(..., description="任务ID")
    status: str = Field("pending", description="任务状态")
    message: str = Field(..., description="提示消息")
    estimated_time: Optional[int] = Field(None, description="预计完成时间（秒）")

# ===== 内容重试请求 =====

class ConceptRetryRequest(BaseModel):
    """概念内容重试请求（通用）"""
    preferences: LearningPreferences = Field(..., description="用户偏好")
    retry_reason: Optional[str] = Field(None, description="重试原因")
    force_regenerate: bool = Field(False, description="是否强制重新生成")

class TutorialRetryRequest(ConceptRetryRequest):
    """教程重试请求"""
    include_code_examples: bool = Field(True, description="是否包含代码示例")

class ResourceRetryRequest(ConceptRetryRequest):
    """资源推荐重试请求"""
    max_resources: int = Field(5, ge=1, le=10, description="最大资源数量")

class QuizRetryRequest(ConceptRetryRequest):
    """测验重试请求"""
    question_count: int = Field(5, ge=3, le=10, description="问题数量")

# ===== 内容重试响应 =====

class ConceptRetryResponse(BaseModel):
    """概念内容重试响应（通用）"""
    success: bool = Field(..., description="是否成功")
    concept_id: str = Field(..., description="概念ID")
    content_type: str = Field(..., description="内容类型: tutorial/resource/quiz")
    message: str = Field(..., description="提示消息")
    task_id: Optional[str] = Field(None, description="异步任务ID（如果是异步）")
    data: Optional[dict] = Field(None, description="生成结果（同步时返回）")

# ===== 任务状态查询 =====

class TaskStatusResponse(BaseModel):
    """任务状态响应"""
    task_id: str
    status: str = Field(..., description="pending/processing/completed/failed")
    progress: Optional[int] = Field(None, ge=0, le=100)
    message: Optional[str] = None
    result: Optional[dict] = None
    error: Optional[str] = None
    created_at: datetime
    updated_at: datetime
```

#### 验收标准
- [ ] 创建10+个Schema模型
- [ ] 所有Schema包含中文描述
- [ ] 包含使用示例（`json_schema_extra`）
- [ ] 通过Pydantic v2验证

---

### 任务2: generation.py CRUD层创建 [Day 1-2]

#### 创建专门的CRUD类

```python
# app/crud/crud_concept.py (新建)
"""
概念相关CRUD操作
"""
from typing import Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.crud.base import BaseCRUD
from app.models.database import RoadmapMetadata

class ConceptCRUD(BaseCRUD[RoadmapMetadata, dict, dict]):
    """
    概念CRUD操作
    
    注意：概念存储在RoadmapMetadata.framework_data中，
    不是独立表，因此需要特殊处理
    """
    
    async def get_concept_from_roadmap(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
    ) -> Optional[tuple[dict, dict, RoadmapMetadata]]:
        """
        从路线图中获取指定概念
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            concept_id: 概念ID
            
        Returns:
            (concept, context, roadmap_metadata) 或 None
        """
        # 获取路线图元数据
        result = await session.execute(
            select(RoadmapMetadata).where(
                RoadmapMetadata.roadmap_id == roadmap_id
            )
        )
        roadmap_metadata = result.scalar_one_or_none()
        
        if not roadmap_metadata:
            return None
        
        framework_data = roadmap_metadata.framework_data
        
        # 遍历查找概念
        for stage_idx, stage in enumerate(framework_data.get("stages", [])):
            for module_idx, module in enumerate(stage.get("modules", [])):
                for concept_idx, concept in enumerate(module.get("concepts", [])):
                    if concept["id"] == concept_id:
                        # 构建上下文
                        context = {
                            "stage": stage,
                            "module": module,
                            "stage_idx": stage_idx,
                            "module_idx": module_idx,
                            "concept_idx": concept_idx,
                        }
                        return concept, context, roadmap_metadata
        
        return None
    
    async def update_concept_status(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
        content_type: str,
        status: str,
        result: Optional[dict] = None,
    ) -> bool:
        """
        更新概念状态
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            concept_id: 概念ID
            content_type: 内容类型 (tutorial/resource/quiz)
            status: 状态 (generating/completed/failed)
            result: 生成结果（completed时提供）
            
        Returns:
            是否成功
        """
        # 获取路线图
        roadmap_result = await session.execute(
            select(RoadmapMetadata).where(
                RoadmapMetadata.roadmap_id == roadmap_id
            )
        )
        roadmap_metadata = roadmap_result.scalar_one_or_none()
        
        if not roadmap_metadata:
            return False
        
        framework_data = roadmap_metadata.framework_data
        
        # 查找并更新概念
        for stage in framework_data.get("stages", []):
            for module in stage.get("modules", []):
                for concept in module.get("concepts", []):
                    if concept["id"] == concept_id:
                        # 更新状态
                        if content_type not in concept:
                            concept[content_type] = {}
                        
                        concept[content_type]["status"] = status
                        
                        if result:
                            concept[content_type]["data"] = result
                        
                        # 保存到数据库
                        roadmap_metadata.framework_data = framework_data
                        session.add(roadmap_metadata)
                        await session.flush()
                        
                        return True
        
        return False

# app/crud/crud_tutorial.py (新建)
"""
教程元数据CRUD操作
"""
from typing import Optional
from sqlalchemy import select, desc
from sqlalchemy.ext.asyncio import AsyncSession

from app.crud.base import BaseCRUD
from app.models.database import TutorialMetadata
from app.schemas.tutorial import TutorialCreate, TutorialUpdate

class TutorialCRUD(BaseCRUD[TutorialMetadata, TutorialCreate, TutorialUpdate]):
    """教程元数据CRUD"""
    
    async def get_by_concept(
        self,
        session: AsyncSession,
        concept_id: str,
    ) -> Optional[TutorialMetadata]:
        """获取概念的最新教程"""
        result = await session.execute(
            select(TutorialMetadata)
            .where(TutorialMetadata.concept_id == concept_id)
            .order_by(desc(TutorialMetadata.created_at))
            .limit(1)
        )
        return result.scalar_one_or_none()
    
    async def get_history(
        self,
        session: AsyncSession,
        concept_id: str,
        limit: int = 10,
    ) -> list[TutorialMetadata]:
        """获取教程历史版本"""
        result = await session.execute(
            select(TutorialMetadata)
            .where(TutorialMetadata.concept_id == concept_id)
            .order_by(desc(TutorialMetadata.created_at))
            .limit(limit)
        )
        return list(result.scalars().all())

# 类似地创建 ResourceCRUD 和 QuizCRUD
```

#### 验收标准
- [ ] 创建ConceptCRUD（处理嵌套JSON数据）
- [ ] 创建TutorialCRUD、ResourceCRUD、QuizCRUD
- [ ] 所有方法包含类型注解和文档字符串
- [ ] 编写单元测试（覆盖率>80%）

---

### 任务3: generation.py Service层提取 [Day 3-4]

#### 创建ConceptService

```python
# app/services/concept_service.py (新建)
"""
概念相关业务逻辑
"""
from typing import Optional
from sqlalchemy.ext.asyncio import AsyncSession
import structlog

from app.crud.crud_concept import ConceptCRUD
from app.crud.crud_roadmap import RoadmapCRUD
from app.services.notification_service import NotificationService
from app.models.domain import Concept

logger = structlog.get_logger()

class ConceptService:
    """
    概念服务
    
    职责：
    - 概念查询和状态管理
    - WebSocket通知
    - 业务逻辑编排
    """
    
    def __init__(
        self,
        concept_crud: ConceptCRUD,
        roadmap_crud: RoadmapCRUD,
        notification_service: NotificationService,
    ):
        self.concept_crud = concept_crud
        self.roadmap_crud = roadmap_crud
        self.notification = notification_service
    
    async def get_concept_from_roadmap(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
    ) -> tuple[Optional[dict], Optional[dict], Optional[Any]]:
        """
        从路线图中获取概念（业务逻辑）
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            concept_id: 概念ID
            
        Returns:
            (concept, context, roadmap_metadata)
        """
        result = await self.concept_crud.get_concept_from_roadmap(
            session, roadmap_id, concept_id
        )
        
        if not result:
            logger.warning(
                "concept_not_found",
                roadmap_id=roadmap_id,
                concept_id=concept_id,
            )
            return None, None, None
        
        concept, context, roadmap_metadata = result
        
        logger.info(
            "concept_retrieved",
            roadmap_id=roadmap_id,
            concept_id=concept_id,
            concept_title=concept.get("title"),
        )
        
        return concept, context, roadmap_metadata
    
    async def update_concept_status(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
        content_type: str,
        status: str,
        result: Optional[dict] = None,
    ) -> bool:
        """
        更新概念状态（业务逻辑 + 通知）
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            concept_id: 概念ID
            content_type: 内容类型
            status: 状态
            result: 结果数据
            
        Returns:
            是否成功
        """
        # 更新数据库
        success = await self.concept_crud.update_concept_status(
            session,
            roadmap_id,
            concept_id,
            content_type,
            status,
            result,
        )
        
        if not success:
            logger.error(
                "concept_status_update_failed",
                roadmap_id=roadmap_id,
                concept_id=concept_id,
            )
            return False
        
        # 发送WebSocket通知
        if status == "generating":
            await self.notification.publish_concept_start(
                roadmap_id=roadmap_id,
                concept_id=concept_id,
                content_type=content_type,
            )
        elif status == "completed":
            await self.notification.publish_concept_complete(
                roadmap_id=roadmap_id,
                concept_id=concept_id,
                content_type=content_type,
                result=result,
            )
        elif status == "failed":
            await self.notification.publish_concept_error(
                roadmap_id=roadmap_id,
                concept_id=concept_id,
                content_type=content_type,
                error=result.get("error") if result else "Unknown error",
            )
        
        logger.info(
            "concept_status_updated",
            roadmap_id=roadmap_id,
            concept_id=concept_id,
            content_type=content_type,
            status=status,
        )
        
        return True
```

#### 创建ContentService（统一重试逻辑）

```python
# app/services/content_service.py (新建)
"""
内容生成服务（统一的重试逻辑）
"""
from typing import Literal
from sqlalchemy.ext.asyncio import AsyncSession
import structlog

from app.crud.crud_tutorial import TutorialCRUD
from app.crud.crud_resource import ResourceCRUD
from app.crud.crud_quiz import QuizCRUD
from app.services.concept_service import ConceptService
from app.agents.tutorial_generator import TutorialGeneratorAgent
from app.agents.resource_recommender import ResourceRecommenderAgent
from app.agents.quiz_generator import QuizGeneratorAgent
from app.schemas.roadmap import ConceptRetryRequest, ConceptRetryResponse

logger = structlog.get_logger()

ContentType = Literal["tutorial", "resource", "quiz"]

class ContentService:
    """
    内容生成服务
    
    职责：
    - 统一的重试逻辑（消除重复代码）
    - Agent调用管理
    - 内容元数据保存
    """
    
    def __init__(
        self,
        concept_service: ConceptService,
        tutorial_crud: TutorialCRUD,
        resource_crud: ResourceCRUD,
        quiz_crud: QuizCRUD,
    ):
        self.concept_service = concept_service
        self.tutorial_crud = tutorial_crud
        self.resource_crud = resource_crud
        self.quiz_crud = quiz_crud
        
        # Agent实例
        self.tutorial_agent = TutorialGeneratorAgent()
        self.resource_agent = ResourceRecommenderAgent()
        self.quiz_agent = QuizGeneratorAgent()
    
    async def retry_content(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
        content_type: ContentType,
        request: ConceptRetryRequest,
    ) -> ConceptRetryResponse:
        """
        统一的内容重试逻辑
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            concept_id: 概念ID
            content_type: 内容类型
            request: 重试请求
            
        Returns:
            重试响应
        """
        # ===== 步骤1: 获取概念 =====
        concept, context, roadmap_metadata = await self.concept_service.get_concept_from_roadmap(
            session, roadmap_id, concept_id
        )
        
        if not concept:
            return ConceptRetryResponse(
                success=False,
                concept_id=concept_id,
                content_type=content_type,
                message="Concept not found",
            )
        
        # ===== 步骤2: 更新状态为generating =====
        await self.concept_service.update_concept_status(
            session,
            roadmap_id,
            concept_id,
            content_type,
            "generating",
        )
        
        try:
            # ===== 步骤3: 调用Agent生成 =====
            if content_type == "tutorial":
                result = await self._generate_tutorial(concept, request)
            elif content_type == "resource":
                result = await self._generate_resource(concept, request)
            elif content_type == "quiz":
                result = await self._generate_quiz(concept, request)
            else:
                raise ValueError(f"Unknown content type: {content_type}")
            
            # ===== 步骤4: 保存结果 =====
            await self._save_content(session, roadmap_id, concept_id, content_type, result)
            
            # ===== 步骤5: 更新状态为completed =====
            await self.concept_service.update_concept_status(
                session,
                roadmap_id,
                concept_id,
                content_type,
                "completed",
                result,
            )
            
            logger.info(
                "content_retry_success",
                roadmap_id=roadmap_id,
                concept_id=concept_id,
                content_type=content_type,
            )
            
            return ConceptRetryResponse(
                success=True,
                concept_id=concept_id,
                content_type=content_type,
                message=f"{content_type.capitalize()} regenerated successfully",
                data=result,
            )
            
        except Exception as e:
            # ===== 步骤6: 错误处理 =====
            logger.error(
                "content_retry_failed",
                roadmap_id=roadmap_id,
                concept_id=concept_id,
                content_type=content_type,
                error=str(e),
                exc_info=True,
            )
            
            await self.concept_service.update_concept_status(
                session,
                roadmap_id,
                concept_id,
                content_type,
                "failed",
                {"error": str(e)},
            )
            
            return ConceptRetryResponse(
                success=False,
                concept_id=concept_id,
                content_type=content_type,
                message=f"Failed to regenerate {content_type}: {str(e)}",
            )
    
    # ===== 私有方法：Agent调用 =====
    
    async def _generate_tutorial(self, concept: dict, request: ConceptRetryRequest) -> dict:
        """生成教程"""
        input_data = {
            "concept": concept,
            "preferences": request.preferences.model_dump(mode='json'),
        }
        return await self.tutorial_agent.execute(input_data)
    
    async def _generate_resource(self, concept: dict, request: ConceptRetryRequest) -> dict:
        """生成资源推荐"""
        input_data = {
            "concept": concept,
            "preferences": request.preferences.model_dump(mode='json'),
        }
        return await self.resource_agent.execute(input_data)
    
    async def _generate_quiz(self, concept: dict, request: ConceptRetryRequest) -> dict:
        """生成测验"""
        input_data = {
            "concept": concept,
            "preferences": request.preferences.model_dump(mode='json'),
        }
        return await self.quiz_agent.execute(input_data)
    
    # ===== 私有方法：保存内容 =====
    
    async def _save_content(
        self,
        session: AsyncSession,
        roadmap_id: str,
        concept_id: str,
        content_type: ContentType,
        result: dict,
    ):
        """保存内容元数据"""
        if content_type == "tutorial":
            await self.tutorial_crud.create(session, obj_in={
                "roadmap_id": roadmap_id,
                "concept_id": concept_id,
                "content": result,
            })
        elif content_type == "resource":
            await self.resource_crud.create(session, obj_in={
                "roadmap_id": roadmap_id,
                "concept_id": concept_id,
                "content": result,
            })
        elif content_type == "quiz":
            await self.quiz_crud.create(session, obj_in={
                "roadmap_id": roadmap_id,
                "concept_id": concept_id,
                "content": result,
            })
```

#### 验收标准
- [ ] ConceptService实现完成（概念查询+状态管理）
- [ ] ContentService实现完成（统一重试逻辑）
- [ ] 消除tutorial/resource/quiz三个重复函数
- [ ] 所有方法包含详细文档字符串
- [ ] 编写单元测试（Mock Agent调用）

---

### 任务4: generation.py API层重构 [Day 5]

#### 重构前后对比

**重构前** (1023行):
```python
# app/api/v1/endpoints/generation.py (旧版)
@router.post("/{roadmap_id}/concepts/{concept_id}/tutorial/retry")
async def retry_tutorial(...):
    """200行业务逻辑"""
    # ... 大量重复代码

@router.post("/{roadmap_id}/concepts/{concept_id}/resources/retry")
async def retry_resources(...):
    """180行业务逻辑"""
    # ... 大量重复代码

@router.post("/{roadmap_id}/concepts/{concept_id}/quiz/retry")
async def retry_quiz(...):
    """180行业务逻辑"""
    # ... 大量重复代码
```

**重构后** (<200行):
```python
# app/api/v1/endpoints/roadmaps.py (新版)
"""
路线图API（重构后）
"""
from fastapi import APIRouter, HTTPException, status
from app.api.v1.deps import (
    CurrentSessionTransaction,
    CurrentContentService,
    CurrentActiveUser,
)
from app.schemas.roadmap import (
    RoadmapGenerateRequest,
    RoadmapGenerateResponse,
    ConceptRetryRequest,
    ConceptRetryResponse,
    TutorialRetryRequest,
    ResourceRetryRequest,
    QuizRetryRequest,
)

router = APIRouter(prefix="/roadmaps", tags=["roadmaps"])

# ===== 路线图生成 =====

@router.post("/generate", response_model=RoadmapGenerateResponse)
async def generate_roadmap(
    request: RoadmapGenerateRequest,
    service: CurrentRoadmapService,
    user: CurrentActiveUser,
):
    """
    生成学习路线图（异步任务）
    
    API层职责：
    1. 参数验证（Pydantic自动）
    2. 调用Service
    3. 返回响应
    """
    result = await service.generate_roadmap(request, user.id)
    return result

# ===== 内容重试 =====

@router.post(
    "/{roadmap_id}/concepts/{concept_id}/tutorial/retry",
    response_model=ConceptRetryResponse,
)
async def retry_tutorial(
    roadmap_id: str,
    concept_id: str,
    request: TutorialRetryRequest,
    service: CurrentContentService,
    session: CurrentSessionTransaction,
):
    """
    重试教程生成
    
    API层精简到10行：
    - 只负责HTTP适配
    - 业务逻辑全在Service层
    """
    result = await service.retry_content(
        session=session,
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        content_type="tutorial",
        request=request,
    )
    
    if not result.success:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=result.message,
        )
    
    return result

@router.post(
    "/{roadmap_id}/concepts/{concept_id}/resources/retry",
    response_model=ConceptRetryResponse,
)
async def retry_resources(
    roadmap_id: str,
    concept_id: str,
    request: ResourceRetryRequest,
    service: CurrentContentService,
    session: CurrentSessionTransaction,
):
    """重试资源推荐"""
    result = await service.retry_content(
        session=session,
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        content_type="resource",
        request=request,
    )
    
    if not result.success:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=result.message,
        )
    
    return result

@router.post(
    "/{roadmap_id}/concepts/{concept_id}/quiz/retry",
    response_model=ConceptRetryResponse,
)
async def retry_quiz(
    roadmap_id: str,
    concept_id: str,
    request: QuizRetryRequest,
    service: CurrentContentService,
    session: CurrentSessionTransaction,
):
    """重试测验生成"""
    result = await service.retry_content(
        session=session,
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        content_type="quiz",
        request=request,
    )
    
    if not result.success:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=result.message,
        )
    
    return result
```

#### 删除旧代码

```bash
# 备份旧文件
cp app/api/v1/endpoints/generation.py app/api/v1/endpoints/generation.py.old

# 删除旧文件
rm app/api/v1/endpoints/generation.py

# 创建新文件
# （使用上面的重构代码）
```

#### 验收标准
- [ ] API层所有函数<50行
- [ ] 无业务逻辑（仅HTTP适配）
- [ ] 使用统一的依赖注入
- [ ] API行为一致性测试通过
- [ ] 性能无劣化

---

### 任务5: modification.py重构 [Day 6-7]

应用generation.py建立的重构范式：

1. **提取Schemas** → `app/schemas/modification.py`
2. **创建CRUD** → 复用ConceptCRUD
3. **创建Service** → `ModificationService`
4. **重构API** → 精简到<50行/endpoint

#### 实施步骤（略，参考generation.py）

---

### 任务6: 重试逻辑统一 [Day 8]

已在ContentService中实现，确保：

- [ ] tutorial/resource/quiz重试逻辑完全统一
- [ ] 消除90%的重复代码
- [ ] 易于扩展新内容类型

---

### 任务7: 集成测试 [Day 9]

```python
# tests/integration/test_roadmap_generation.py

@pytest.mark.asyncio
async def test_retry_tutorial_e2e(client, test_user, test_roadmap):
    """端到端测试：重试教程生成"""
    # 1. 发起重试请求
    response = client.post(
        f"/api/v1/roadmaps/{test_roadmap.id}/concepts/concept-1/tutorial/retry",
        json={
            "preferences": {
                "learning_goal": "Learn Python",
                "current_level": "beginner",
            }
        },
        headers={"Authorization": f"Bearer {test_user.token}"}
    )
    
    # 2. 验证响应
    assert response.status_code == 200
    data = response.json()
    assert data["success"] is True
    assert data["concept_id"] == "concept-1"
    assert data["content_type"] == "tutorial"
    
    # 3. 验证数据库
    async with get_session() as session:
        tutorial_crud = TutorialCRUD(TutorialMetadata)
        tutorial = await tutorial_crud.get_by_concept(session, "concept-1")
        assert tutorial is not None
```

---

## 🔍 验收标准

### 代码量对比

| 模块 | 重构前 | 重构后 | 减少 |
|------|--------|--------|------|
| generation.py | 1023行 | 200行 | **80%** |
| modification.py | 800行 | 150行 | **81%** |
| 辅助函数 | 400行 | 0行 | **100%** |
| **总计** | **2223行** | **350行** | **84%** |

### 职责分离验证

```bash
# 检查API层行数
find app/api/v1/endpoints/ -name "*.py" -exec wc -l {} \; | awk '{if ($1 > 300) print $0}'
# 预期：无输出（所有文件<300行）

# 检查函数行数
ruff check app/api/ --select C901  # 圈复杂度检查
# 预期：无高复杂度函数
```

### 测试覆盖率

```bash
pytest tests/ --cov=app/services --cov=app/crud --cov-report=html
# 预期：覆盖率>80%
```

---

## ⚠️ 风险控制

### 风险1: API行为变化

**缓解措施**:
- 编写API行为一致性测试
- 对比重构前后的响应格式
- 灰度发布

### 风险2: 性能劣化

**缓解措施**:
- 性能基准测试
- 监控响应时间
- 优化SQL查询

### 风险3: 引入新Bug

**缓解措施**:
- 高测试覆盖率（>80%）
- 完善的Code Review
- 充分的集成测试

---

## ⏱️ 时间估算

| 任务 | 开发 | 测试 | 总计 |
|------|------|------|------|
| Schemas提取 | 6h | 2h | 8h |
| CRUD层创建 | 6h | 2h | 8h |
| Service层提取 | 8h | 4h | 12h |
| API层重构 | 4h | 2h | 6h |
| modification重构 | 6h | 4h | 10h |
| 重试逻辑统一 | 4h | 2h | 6h |
| 集成测试 | - | 6h | 6h |
| 文档更新 | 4h | - | 4h |
| **总计** | **38h** | **22h** | **60h** |

**建议工期**: 10个工作日（按每天6小时有效工作时间计算）

---

## 📊 依赖关系

### 前置条件

- ✅ 阶段0已完成（紧急止血）
- ✅ 阶段1已完成（架构基础建设）
- ✅ schemas/和crud/目录已创建

### 后续阶段

完成后进入：
- **阶段3**: 全面架构迁移（应用重构范式到所有endpoints）

---

## ✅ 完成检查清单

- [ ] generation.py已重构（1023行→200行）
- [ ] modification.py已重构
- [ ] 重试逻辑已统一（ContentService）
- [ ] 所有API层<50行/endpoint
- [ ] 测试覆盖率>80%
- [ ] API行为一致性测试通过
- [ ] 性能测试无劣化
- [ ] 重构范式文档已编写
- [ ] 团队培训已完成

---

**文档版本**: v1.0  
**创建日期**: 2026-01-05  
**预计完成**: 2026-01-19  
**负责人**: 核心开发组  
**审核人**: 架构负责人

