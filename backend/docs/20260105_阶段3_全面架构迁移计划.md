# 阶段3：全面架构迁移计划

> **优先级**: P1-Medium  
> **工期**: 2-3周（15个工作日）  
> **目标**: 将剩余30个endpoints迁移到新架构，清理遗留代码

---

## 📋 执行摘要

### 核心目标

将阶段2建立的重构范式应用到所有剩余模块：

1. **迁移30个endpoints** - 应用API/Service/CRUD分层
2. **清理repositories/** - 移除1372行的roadmap_repo.py等旧代码
3. **API层全面瘦身** - 所有文件<300行，函数<50行
4. **测试覆盖率达标** - 整体覆盖率>80%

### 关键成果

- ✅ **100%架构对齐**：所有代码符合新规范
- ✅ **代码量减少60%**：总代码从~15000行降至~6000行
- ✅ **可维护性提升**：新人上手时间从2周降至3天
- ✅ **技术债清零**：遗留代码完全清理

### 工期分配

**Week 1**:
- Day 1-2: retrieval、status、task相关endpoints（6个，12小时）
- Day 3-4: tutorial、resource、quiz专属endpoints（6个，12小时）
- Day 5: mentor、progress相关endpoints（4个，6小时）

**Week 2**:
- Day 6-7: tech_assessment、approval相关endpoints（4个，10小时）
- Day 8-9: 剩余endpoints + 辅助模块（10个，12小时）
- Day 10: 第一轮集成测试（6小时）

**Week 3**:
- Day 11-12: 清理db/repositories/（移除旧代码，10小时）
- Day 13: 统一导入路径 + 删除重复代码（6小时）
- Day 14: 全量回归测试（6小时）
- Day 15: 文档更新 + 团队培训（4小时）

---

## 🎯 背景与动机

### 迁移必要性

阶段2已证明新架构的优势：
- generation.py从1023行降至200行（**减少80%**）
- 业务逻辑清晰，易于测试
- 开发效率显著提升

现在需要将这套方法论推广到全部32个endpoints。

### 迁移对象统计

| 模块 | Endpoint数量 | 复杂度 | 优先级 | 预计工期 |
|------|-------------|--------|--------|----------|
| **已完成** |  |  |  |  |
| generation | 6 | 高 | P0 | ✅ 完成 |
| modification | 4 | 高 | P0 | ✅ 完成 |
| **待迁移** |  |  |  |  |
| retrieval | 3 | 中 | P1 | 4h |
| status | 2 | 低 | P1 | 2h |
| task | 1 | 低 | P1 | 2h |
| tutorial | 2 | 中 | P1 | 4h |
| resource | 2 | 中 | P1 | 4h |
| quiz | 2 | 中 | P1 | 4h |
| mentor | 3 | 中 | P1 | 6h |
| progress | 2 | 低 | P1 | 3h |
| tech_assessment | 4 | 高 | P1 | 8h |
| approval | 2 | 中 | P1 | 4h |
| 其他 | 10 | 低-中 | P2 | 12h |
| **总计** | **32** | - | - | **53h** |

---

## ✅ 详细任务清单

### Week 1: 核心业务模块迁移

#### Day 1-2: retrieval + status + task [12h]

**1. retrieval.py重构**

当前问题：
- 包含3个endpoint，总计约400行
- 直接使用RepositoryFactory
- 返回ORM对象而非Schema

重构步骤：

```python
# 步骤1: 创建Schemas
# app/schemas/retrieval.py
class RoadmapRetrieveResponse(BaseModel):
    """路线图检索响应"""
    roadmap_id: str
    title: str
    description: Optional[str]
    framework_data: dict
    progress: int
    created_at: datetime
    updated_at: datetime

class RoadmapListRequest(BaseModel):
    """路线图列表请求"""
    user_id: str
    page: int = Field(1, ge=1)
    page_size: int = Field(20, ge=1, le=100)
    sort_by: str = Field("created_at", regex="^(created_at|updated_at|title)$")
    order: str = Field("desc", regex="^(asc|desc)$")

# 步骤2: 复用CRUD
# 使用 RoadmapCRUD（已在阶段1创建）

# 步骤3: 创建Service（如需要）
# app/services/retrieval_service.py
class RetrievalService:
    """路线图检索服务"""
    async def get_user_roadmaps(self, user_id: str, pagination: PaginationParams):
        # 业务逻辑

# 步骤4: 重构API
# app/api/v1/endpoints/roadmaps.py (合并到主文件)
@router.get("/", response_model=PaginatedResponse[RoadmapSummary])
async def list_roadmaps(
    request: RoadmapListRequest,
    service: CurrentRetrievalService,
):
    """列出用户的路线图（<20行）"""
    return await service.get_user_roadmaps(request.user_id, request)
```

验收标准：
- [ ] API层<20行/endpoint
- [ ] 使用统一的依赖注入
- [ ] 返回Pydantic Schema而非ORM
- [ ] 行为一致性测试通过

**2. status.py重构**

类似处理，创建：
- `app/schemas/status.py`
- 复用`TaskCRUD`
- 创建`StatusService`（如需要）
- 重构API层

**3. task.py重构**

合并到`roadmaps.py`或`status.py`，避免文件过于分散。

---

#### Day 3-4: tutorial + resource + quiz专属endpoints [12h]

这三个模块当前分散在不同文件中，重构后统一到`content.py`：

```python
# app/api/v1/endpoints/content.py (新建)
"""
内容相关API（教程、资源、测验）
"""
from fastapi import APIRouter
from app.api.v1.deps import CurrentContentService, CurrentSession
from app.schemas.tutorial import TutorialDetailResponse
from app.schemas.resource import ResourceDetailResponse
from app.schemas.quiz import QuizDetailResponse

router = APIRouter(prefix="/content", tags=["content"])

# ===== 教程相关 =====

@router.get("/tutorials/{tutorial_id}", response_model=TutorialDetailResponse)
async def get_tutorial(
    tutorial_id: str,
    service: CurrentContentService,
    session: CurrentSession,
):
    """获取教程详情"""
    return await service.get_tutorial(session, tutorial_id)

@router.get("/tutorials/{tutorial_id}/history")
async def get_tutorial_history(
    tutorial_id: str,
    service: CurrentContentService,
    session: CurrentSession,
):
    """获取教程历史版本"""
    return await service.get_tutorial_history(session, tutorial_id)

# ===== 资源推荐相关 =====

@router.get("/resources/{resource_id}", response_model=ResourceDetailResponse)
async def get_resource(
    resource_id: str,
    service: CurrentContentService,
    session: CurrentSession,
):
    """获取资源推荐详情"""
    return await service.get_resource(session, resource_id)

# ===== 测验相关 =====

@router.get("/quizzes/{quiz_id}", response_model=QuizDetailResponse)
async def get_quiz(
    quiz_id: str,
    service: CurrentContentService,
    session: CurrentSession,
):
    """获取测验详情"""
    return await service.get_quiz(session, quiz_id)
```

验收标准：
- [ ] 三个模块统一到`content.py`
- [ ] 复用ContentService
- [ ] 所有endpoint<30行
- [ ] API文档自动生成正确

---

#### Day 5: mentor + progress [6h]

**1. mentor.py重构**

当前问题：
- 包含伴学聊天、笔记管理等功能
- 约500行代码
- 部分逻辑混乱

重构步骤：

```python
# 步骤1: 创建Schemas
# app/schemas/mentor.py
class ChatRequest(BaseModel):
    """聊天请求"""
    roadmap_id: str
    concept_id: Optional[str] = None
    message: str = Field(..., min_length=1, max_length=2000)
    context: Optional[dict] = None

class ChatResponse(BaseModel):
    """聊天响应"""
    message_id: str
    reply: str
    suggested_actions: Optional[list[str]] = None
    references: Optional[list[dict]] = None

class NoteCreateRequest(BaseModel):
    """笔记创建请求"""
    roadmap_id: str
    concept_id: str
    content: str = Field(..., min_length=1, max_length=10000)
    tags: Optional[list[str]] = None

# 步骤2: 创建CRUD
# app/crud/crud_note.py
class NoteCRUD(BaseCRUD[Note, NoteCreate, NoteUpdate]):
    async def get_by_concept(self, session, concept_id):
        # ...

# 步骤3: 创建Service
# app/services/mentor_service.py
class MentorService:
    """伴学服务"""
    async def chat(self, request: ChatRequest):
        # 调用QAAgent
        # 保存聊天记录
        # 返回响应
    
    async def create_note(self, request: NoteCreateRequest):
        # 保存笔记
        # 关联概念

# 步骤4: 重构API
# app/api/v1/endpoints/mentor.py (精简版)
@router.post("/chat", response_model=ChatResponse)
async def chat(
    request: ChatRequest,
    service: CurrentMentorService,
):
    """伴学聊天（<15行）"""
    return await service.chat(request)
```

**2. progress.py重构**

类似处理，创建：
- `app/schemas/progress.py`
- `app/crud/crud_progress.py`
- `app/services/progress_service.py`
- 重构API层

---

### Week 2: 高级功能模块迁移

#### Day 6-7: tech_assessment [10h]

这是最复杂的模块之一，包含：
- 技术能力评估
- 问题生成
- 答案验证
- 报告生成

重构策略：

```python
# app/services/tech_assessment_service.py
class TechAssessmentService:
    """技术评估服务"""
    
    async def create_assessment(self, user_id: str, tech_stack: list[str]):
        """创建评估任务"""
        # 1. 验证技术栈
        # 2. 调用TechAssessmentPlannerAgent
        # 3. 创建评估任务
        # 4. 返回任务ID
    
    async def generate_questions(self, assessment_id: str):
        """生成评估问题"""
        # 1. 获取评估计划
        # 2. 调用QuestionGeneratorAgent
        # 3. 保存问题
        # 4. 更新评估状态
    
    async def submit_answers(self, assessment_id: str, answers: dict):
        """提交答案并评分"""
        # 1. 验证答案
        # 2. 调用CapabilityAnalyzerAgent
        # 3. 生成报告
        # 4. 返回结果
```

验收标准：
- [ ] 拆分为多个Service方法
- [ ] Agent调用清晰
- [ ] API层<30行/endpoint
- [ ] 包含完整的测试用例

---

#### Day 8-9: approval + 剩余endpoints [12h]

**1. approval.py重构**

路线图审核功能，创建：
- `app/schemas/approval.py`
- `app/services/approval_service.py`
- 重构API层

**2. 其他小型endpoints**

包括：
- health check
- version info
- webhook handlers
- 等

迁移原则：
- 简单endpoints可直接在API层实现（无需Service）
- 复用现有CRUD
- 统一错误处理

---

### Week 3: 清理与验收

#### Day 11-12: 清理db/repositories/ [10h]

**目标**: 移除所有旧的Repository代码

```bash
# 检查哪些Repository还在使用
grep -r "from app.db.repositories" app/ --include="*.py"

# 预期：只有旧文件引用，新代码应使用app.crud

# 安全删除步骤：
# 1. 确认所有CRUD迁移完成
# 2. 运行全量测试
# 3. 删除repositories/目录
rm -rf app/db/repositories/

# 4. 更新导入（如有遗漏）
# 使用ruff或其他工具自动修复导入
```

**重点清理对象**:

1. **roadmap_repo.py** (1372行)
   - 已拆分为：RoadmapCRUD、ConceptCRUD、TutorialCRUD、ResourceCRUD、QuizCRUD
   - 确认所有方法已迁移
   - 删除文件

2. **task_repo.py**
   - 已迁移到：TaskCRUD
   - 删除文件

3. **user_repo.py**
   - 已迁移到：UserCRUD
   - 删除文件

4. **其他Repository文件**
   - 逐一确认迁移状态
   - 批量删除

验收标准：
- [ ] `app/db/repositories/`目录已删除
- [ ] 无任何代码引用旧Repository
- [ ] 全量测试通过

---

#### Day 13: 统一导入路径 + 删除重复代码 [6h]

**1. 统一导入路径**

```python
# 检查导入一致性
ruff check app/ --select I  # Import order检查

# 标准导入顺序：
# 1. 标准库
# 2. 第三方库
# 3. app.schemas
# 4. app.crud
# 5. app.services
# 6. app.models

# 示例：
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.schemas.roadmap import RoadmapGenerateRequest
from app.crud.crud_roadmap import RoadmapCRUD
from app.services.roadmap_service import RoadmapService
from app.models.database import RoadmapMetadata
```

**2. 删除重复代码**

```bash
# 使用jscpd检测重复代码
npx jscpd app/ --min-lines 10 --format python

# 重点检查：
# - 相似的Schema定义
# - 重复的CRUD方法
# - 相似的Service逻辑
```

常见重复代码：
- 多个Service中重复的通知逻辑 → 提取到NotificationService
- 重复的参数验证 → 使用Pydantic validator
- 重复的错误处理 → 使用统一的异常处理器

---

#### Day 14: 全量回归测试 [6h]

**1. 单元测试**

```bash
# 运行所有单元测试
pytest tests/unit/ -v --cov=app --cov-report=html

# 检查覆盖率
# 目标：>80%
```

**2. 集成测试**

```bash
# 运行所有集成测试
pytest tests/integration/ -v

# 重点测试：
# - 所有API endpoint
# - Service层业务逻辑
# - CRUD层数据访问
```

**3. E2E测试**

```bash
# 运行端到端测试
pytest tests/e2e/ -v

# 测试完整业务流程：
# - 用户注册 → 生成路线图 → 学习进度 → 伴学聊天
```

**4. 性能测试**

```bash
# 使用locust进行压力测试
locust -f tests/performance/locustfile.py --host=http://localhost:8000

# 关键指标：
# - P95响应时间 < 500ms
# - 吞吐量 > 100 QPS
# - 错误率 < 0.1%
```

---

#### Day 15: 文档更新 + 团队培训 [4h]

**1. 更新架构文档**

```markdown
# backend/docs/ARCHITECTURE.md (更新)

## 目录结构

\`\`\`
app/
├── api/v1/endpoints/    # API层（<300行/文件）
│   ├── roadmaps.py      # 路线图相关
│   ├── content.py       # 内容相关
│   ├── mentor.py        # 伴学相关
│   └── ...
├── schemas/             # Pydantic Schemas
├── services/            # 业务逻辑层
├── crud/                # 数据访问层
└── models/              # 数据库模型
\`\`\`

## 分层职责

### API层
- **职责**: HTTP协议适配
- **约束**: 不包含业务逻辑，<50行/函数
- **示例**: 见`app/api/v1/endpoints/roadmaps.py`

### Service层
- **职责**: 核心业务逻辑
- **约束**: 可复用，易测试，<100行/函数
- **示例**: 见`app/services/roadmap_service.py`

### CRUD层
- **职责**: 纯粹的数据库操作
- **约束**: 无业务逻辑，继承BaseCRUD
- **示例**: 见`app/crud/crud_roadmap.py`
```

**2. 更新API文档**

```bash
# 重新生成OpenAPI文档
python -m app.main  # 启动服务
curl http://localhost:8000/openapi.json > docs/openapi.json

# 生成ReDoc文档
redoc-cli bundle docs/openapi.json -o docs/api.html
```

**3. 团队培训**

准备培训材料：
- 新架构介绍 (PPT)
- 代码示例 (Jupyter Notebook)
- 最佳实践 (Markdown)
- Q&A文档

---

## 🔍 验收标准

### 代码量统计

```bash
# 统计迁移前后的代码行数
cloc app/ --by-file --csv > before.csv  # 迁移前
cloc app/ --by-file --csv > after.csv   # 迁移后

# 预期变化：
# - 总行数：~15000 → ~6000（减少60%）
# - 平均文件行数：~400 → ~150（减少62%）
# - 最大文件行数：1372 → <300（减少78%）
```

### 架构对齐度

| 检查项 | 标准 | 验收方法 |
|--------|------|----------|
| API层行数 | <300行/文件 | `find app/api/ -name "*.py" -exec wc -l {} \;` |
| 函数复杂度 | 圈复杂度<10 | `ruff check app/ --select C901` |
| 类型覆盖率 | 100% | `mypy app/ --strict` |
| 测试覆盖率 | >80% | `pytest --cov=app` |
| 导入规范 | 符合标准 | `ruff check app/ --select I` |

### 功能完整性

- [ ] 所有32个endpoints已迁移
- [ ] API行为与迁移前一致
- [ ] 所有集成测试通过
- [ ] 性能无劣化（<5%）

### 代码质量

- [ ] 无pylint警告
- [ ] 无mypy类型错误
- [ ] 无重复代码（jscpd检查）
- [ ] 所有函数包含文档字符串

---

## ⚠️ 风险控制

### 风险1: 遗漏未迁移的代码

**缓解措施**:
- 使用grep全局搜索旧导入
- Code Review严格把关
- 自动化测试覆盖

**检查脚本**:
```bash
#!/bin/bash
# scripts/check_migration.sh

echo "检查是否还有旧Repository引用..."
grep -r "from app.db.repositories" app/ --include="*.py" || echo "✅ 无旧引用"

echo "检查是否还有RepositoryFactory引用..."
grep -r "RepositoryFactory" app/api/ --include="*.py" || echo "✅ 无Factory引用"

echo "检查是否还有直接实例化Agent..."
grep -r "Agent()" app/api/ --include="*.py" || echo "✅ 无直接实例化"
```

### 风险2: 测试覆盖不足

**缓解措施**:
- 每个模块迁移后立即编写测试
- 使用pytest-cov监控覆盖率
- 不达标不合并

### 风险3: 性能回归

**缓解措施**:
- 迁移前后性能基准测试
- 监控关键指标（响应时间、吞吐量）
- 优化N+1查询

**性能基准**:
```python
# tests/performance/benchmark.py
import pytest
import time

@pytest.mark.benchmark
def test_list_roadmaps_performance(client, test_user):
    """基准测试：列出路线图"""
    start = time.time()
    response = client.get(
        "/api/v1/roadmaps",
        headers={"Authorization": f"Bearer {test_user.token}"}
    )
    elapsed = time.time() - start
    
    assert response.status_code == 200
    assert elapsed < 0.5  # 响应时间<500ms
```

---

## 🧪 测试策略

### 测试金字塔

```
       /\
      /E2E\          5%  - 端到端测试
     /------\
    /Integr-\       15% - 集成测试
   /----------\
  /Unit Tests \    80% - 单元测试
 /--------------\
```

### 单元测试（80%）

重点测试：
- CRUD层所有方法
- Service层业务逻辑
- Schema验证逻辑

```python
# tests/unit/test_roadmap_crud.py
@pytest.mark.asyncio
async def test_roadmap_crud_create(session):
    """测试：创建路线图"""
    crud = RoadmapCRUD(RoadmapMetadata)
    roadmap = await crud.create(session, obj_in={
        "roadmap_id": "test-123",
        "user_id": "user-1",
        "title": "Test Roadmap",
    })
    assert roadmap.id is not None
    assert roadmap.title == "Test Roadmap"
```

### 集成测试（15%）

重点测试：
- API endpoint完整流程
- Service与CRUD交互
- 数据库事务

```python
# tests/integration/test_roadmap_api.py
def test_create_roadmap_integration(client, test_user):
    """集成测试：创建路线图"""
    response = client.post(
        "/api/v1/roadmaps/generate",
        json={"user_id": test_user.id, "preferences": {...}},
        headers={"Authorization": f"Bearer {test_user.token}"}
    )
    assert response.status_code == 200
    data = response.json()
    assert "task_id" in data
```

### E2E测试（5%）

重点测试：
- 完整业务流程
- 跨模块交互
- 真实场景模拟

```python
# tests/e2e/test_roadmap_lifecycle.py
@pytest.mark.e2e
def test_roadmap_full_lifecycle(client, test_user):
    """端到端测试：路线图完整生命周期"""
    # 1. 生成路线图
    response = client.post("/api/v1/roadmaps/generate", ...)
    task_id = response.json()["task_id"]
    
    # 2. 等待生成完成
    wait_for_task(client, task_id)
    
    # 3. 获取路线图
    roadmap = client.get(f"/api/v1/roadmaps/{task_id}")
    
    # 4. 重试教程
    client.post(f"/api/v1/roadmaps/{task_id}/concepts/.../tutorial/retry", ...)
    
    # 5. 伴学聊天
    client.post("/api/v1/mentor/chat", ...)
    
    # 6. 更新进度
    client.post("/api/v1/progress/update", ...)
```

---

## ⏱️ 时间估算

| Week | 任务 | 开发 | 测试 | 总计 |
|------|------|------|------|------|
| Week 1 | 核心业务模块迁移 | 20h | 10h | 30h |
| Week 2 | 高级功能模块迁移 | 16h | 6h | 22h |
| Week 3 | 清理与验收 | 12h | 10h | 22h |
| **总计** | - | **48h** | **26h** | **74h** |

**实际工期**: 
- 单人：15个工作日
- 双人并行：8-10个工作日
- 三人并行：6-7个工作日

---

## 📊 依赖关系

### 前置条件

- ✅ 阶段0已完成（紧急止血）
- ✅ 阶段1已完成（架构基础建设）
- ✅ 阶段2已完成（核心模块重构）

### 后续阶段

完成后进入：
- **阶段4**: 性能与可观测性优化

### 并行策略

可以多人并行开发：
- 开发者A: retrieval + status + task
- 开发者B: tutorial + resource + quiz
- 开发者C: mentor + progress
- 开发者D: tech_assessment + approval

---

## ✅ 完成检查清单

### 代码迁移
- [ ] 30个endpoints已迁移
- [ ] `db/repositories/`目录已删除
- [ ] 所有API层<300行/文件
- [ ] 所有函数<50行

### 测试覆盖
- [ ] 单元测试覆盖率>80%
- [ ] 所有集成测试通过
- [ ] E2E测试通过
- [ ] 性能测试无劣化

### 代码质量
- [ ] mypy类型检查通过
- [ ] ruff代码检查通过
- [ ] 无重复代码
- [ ] 所有函数包含文档字符串

### 文档更新
- [ ] 架构文档已更新
- [ ] API文档已重新生成
- [ ] 迁移指南已完善
- [ ] 团队培训已完成

### 生产就绪
- [ ] 所有环境变量已配置
- [ ] 数据库迁移脚本已准备
- [ ] 监控告警已配置
- [ ] 回滚方案已准备

---

## 📝 里程碑

### 里程碑1: Week 1完成（Day 5）
- ✅ 6个核心业务模块迁移完成
- ✅ 集成测试通过
- 🎯 代码量减少约30%

### 里程碑2: Week 2完成（Day 10）
- ✅ 所有业务模块迁移完成
- ✅ 第一轮集成测试通过
- 🎯 代码量减少约50%

### 里程碑3: Week 3完成（Day 15）
- ✅ 旧代码完全清理
- ✅ 全量测试通过
- ✅ 文档更新完成
- 🎯 **架构重构100%完成**

---

**文档版本**: v1.0  
**创建日期**: 2026-01-05  
**预计完成**: 2026-01-26  
**负责人**: 全体后端团队  
**审核人**: 技术总监

