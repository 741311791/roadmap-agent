# 阶段1：架构基础建设计划

> **优先级**: P1-High  
> **工期**: 1周（5个工作日）  
> **目标**: 建立标准化架构基础，为后续大规模重构铺路

---

## 📋 执行摘要

### 核心目标

建立符合现代Python API开发规范的标准化架构基础：

1. **Schemas层** - 创建统一的API请求/响应模型管理
2. **CRUD层** - 实现泛型数据访问层，替代Repository模式
3. **Session分离** - 区分读写Session，明确事务边界
4. **依赖注入** - 统一DI规范，消除实例化混乱
5. **开发规范** - 编写Code Review Checklist和迁移指南

### 关键特点

- ✅ **零风险**：纯新增代码，不修改现有功能
- ✅ **零阻塞**：不影响正常业务迭代
- ✅ **高复用**：BaseCRUD泛型类，一次编写处处使用
- ✅ **标准化**：对齐FastAPI最佳实践

### 工期分配

- Day 1: 创建schemas/目录结构 + 编写示例Schemas（6小时）
- Day 2: 创建crud/目录 + 实现BaseCRUD泛型类（6小时）
- Day 3: 实现Session读写分离（4小时）
- Day 4: 编写统一依赖注入规范（4小时）
- Day 5: 编写开发规范文档 + Code Review Checklist（4小时）

---

## 🎯 背景与动机

### 当前架构问题

基于《后端架构重构可行性分析报告》的发现：

#### 问题1: Schemas管理混乱
```python
# 当前状态：Schemas分散在各个endpoint文件中
# app/api/v1/endpoints/generation.py
class RetryContentRequest(BaseModel):
    preferences: LearningPreferences = Field(...)

class RetryContentResponse(BaseModel):
    success: bool = Field(...)

# app/api/v1/endpoints/tutorial.py
class TutorialRequest(BaseModel):  # ❌ 与RetryContentRequest重复
    preferences: LearningPreferences = Field(...)
```

**后果**:
- 模型重复定义，维护成本高
- 缺少统一的导入路径
- API文档生成混乱

#### 问题2: Repository层过于庞大
```python
# app/db/repositories/roadmap_repo.py (1372行)
class RoadmapRepository:
    # 任务管理 (89行)
    async def create_task(...)
    async def get_task(...)
    
    # 路线图元数据 (269行)
    async def save_roadmap_metadata(...)
    
    # 教程元数据 (252行)
    async def save_tutorial_metadata(...)
    
    # ... 违反单一职责原则
```

#### 问题3: 依赖注入不统一
```python
# 方式1：直接注入AsyncSession
@router.get("/users/{user_id}")
async def get_user(db: AsyncSession = Depends(get_db)):
    repo = UserRepository(db)

# 方式2：注入RepositoryFactory
@router.post("/generate")
async def generate(repo_factory: RepositoryFactory = Depends(...)):
    async with repo_factory.create_session() as session:
        # ...

# 方式3：直接实例化（最糟糕）
async def retry_tutorial(...):
    agent = TutorialGeneratorAgent()  # ❌ 无依赖注入
```

### 为什么现在建设

1. **前置条件已满足**：阶段0紧急问题已修复，系统稳定
2. **不阻塞业务**：纯新增，现有代码继续运行
3. **为重构铺路**：阶段2核心重构依赖本阶段的基础设施
4. **降低迁移成本**：一次性建立标准，避免反复返工

---

## ✅ 详细任务清单

### 任务1: 创建Schemas目录结构 [Day 1]

#### 目标目录结构

```
backend/app/schemas/
├── __init__.py           # 统一导出
├── common.py             # 通用Schemas
├── roadmap.py            # 路线图相关
├── concept.py            # 概念相关
├── tutorial.py           # 教程相关
├── resource.py           # 资源推荐相关
├── quiz.py               # 测验相关
├── mentor.py             # 伴学相关
├── user.py               # 用户相关
├── progress.py           # 进度相关
└── tech_assessment.py    # 技术评估相关
```

#### 实现示例

**1. 通用Schemas** (`app/schemas/common.py`):

```python
"""
通用Schemas - 跨模块复用的基础模型
"""
from pydantic import BaseModel, Field, ConfigDict
from typing import Generic, TypeVar, Optional
from datetime import datetime

# ===== 通用响应模型 =====

class ErrorDetail(BaseModel):
    """错误详情"""
    code: str = Field(..., description="错误码")
    message: str = Field(..., description="错误消息")
    field: Optional[str] = Field(None, description="错误字段（表单验证时使用）")

class ErrorResponse(BaseModel):
    """统一错误响应"""
    error: ErrorDetail
    request_id: Optional[str] = Field(None, description="请求追踪ID")
    timestamp: datetime = Field(default_factory=datetime.utcnow)

class SuccessResponse(BaseModel):
    """通用成功响应"""
    success: bool = Field(True, description="操作是否成功")
    message: str = Field(..., description="提示消息")
    data: Optional[dict] = Field(None, description="返回数据")

# ===== 分页模型 =====

T = TypeVar('T')

class PaginationParams(BaseModel):
    """分页参数"""
    page: int = Field(1, ge=1, description="页码（从1开始）")
    page_size: int = Field(20, ge=1, le=100, description="每页数量")
    
    @property
    def skip(self) -> int:
        """计算跳过的记录数"""
        return (self.page - 1) * self.page_size
    
    @property
    def limit(self) -> int:
        """返回限制数量"""
        return self.page_size

class PaginatedResponse(BaseModel, Generic[T]):
    """分页响应"""
    items: list[T] = Field(..., description="数据列表")
    total: int = Field(..., description="总记录数")
    page: int = Field(..., description="当前页码")
    page_size: int = Field(..., description="每页数量")
    total_pages: int = Field(..., description="总页数")
    
    model_config = ConfigDict(from_attributes=True)
    
    @classmethod
    def create(cls, items: list[T], total: int, pagination: PaginationParams):
        """工厂方法：创建分页响应"""
        return cls(
            items=items,
            total=total,
            page=pagination.page,
            page_size=pagination.page_size,
            total_pages=(total + pagination.page_size - 1) // pagination.page_size,
        )

# ===== 任务状态模型 =====

class TaskStatus(BaseModel):
    """异步任务状态"""
    task_id: str = Field(..., description="任务ID")
    status: str = Field(..., description="任务状态: pending/processing/completed/failed")
    progress: Optional[int] = Field(None, ge=0, le=100, description="进度百分比")
    message: Optional[str] = Field(None, description="状态消息")
    result: Optional[dict] = Field(None, description="任务结果（completed时返回）")
    error: Optional[str] = Field(None, description="错误信息（failed时返回）")
    created_at: datetime = Field(..., description="创建时间")
    updated_at: datetime = Field(..., description="更新时间")
```

**2. 路线图Schemas** (`app/schemas/roadmap.py`):

```python
"""
路线图相关Schemas
"""
from pydantic import BaseModel, Field, field_validator
from typing import Optional
from datetime import datetime

from app.models.domain import LearningPreferences

# ===== 请求模型 =====

class RoadmapGenerateRequest(BaseModel):
    """路线图生成请求"""
    user_id: str = Field(..., description="用户ID")
    preferences: LearningPreferences = Field(..., description="学习偏好")
    
    model_config = {"json_schema_extra": {
        "example": {
            "user_id": "user-123",
            "preferences": {
                "learning_goal": "成为全栈开发工程师",
                "current_level": "beginner",
                "time_commitment": "10小时/周",
            }
        }
    }}

class RoadmapUpdateRequest(BaseModel):
    """路线图更新请求"""
    title: Optional[str] = Field(None, description="路线图标题")
    description: Optional[str] = Field(None, description="路线图描述")
    is_public: Optional[bool] = Field(None, description="是否公开")

class ConceptRetryRequest(BaseModel):
    """概念内容重试请求"""
    preferences: LearningPreferences = Field(..., description="用户偏好")
    retry_reason: Optional[str] = Field(None, description="重试原因")

# ===== 响应模型 =====

class RoadmapGenerateResponse(BaseModel):
    """路线图生成响应"""
    task_id: str = Field(..., description="任务ID")
    status: str = Field(..., description="任务状态")
    message: str = Field(..., description="提示消息")
    estimated_time: Optional[int] = Field(None, description="预计完成时间（秒）")

class ConceptRetryResponse(BaseModel):
    """概念内容重试响应"""
    success: bool = Field(..., description="是否成功")
    concept_id: str = Field(..., description="概念ID")
    content_type: str = Field(..., description="内容类型: tutorial/resource/quiz")
    message: str = Field(..., description="提示消息")
    data: Optional[dict] = Field(None, description="生成结果")

class RoadmapSummary(BaseModel):
    """路线图摘要（列表展示）"""
    roadmap_id: str = Field(..., description="路线图ID")
    title: str = Field(..., description="标题")
    description: Optional[str] = Field(None, description="描述")
    cover_image_url: Optional[str] = Field(None, description="封面图URL")
    progress: int = Field(..., ge=0, le=100, description="学习进度")
    total_concepts: int = Field(..., ge=0, description="总概念数")
    completed_concepts: int = Field(..., ge=0, description="已完成概念数")
    created_at: datetime = Field(..., description="创建时间")
    updated_at: datetime = Field(..., description="更新时间")
    
    model_config = {"from_attributes": True}

class RoadmapDetail(BaseModel):
    """路线图详情（包含完整框架）"""
    roadmap_id: str
    title: str
    description: Optional[str] = None
    cover_image_url: Optional[str] = None
    framework_data: dict = Field(..., description="路线图框架数据")
    user_id: str
    created_at: datetime
    updated_at: datetime
    
    model_config = {"from_attributes": True}

# ===== 列表响应 =====

class RoadmapListResponse(BaseModel):
    """路线图列表响应"""
    roadmaps: list[RoadmapSummary]
    total: int = Field(..., description="总数")
    page: int = Field(..., description="当前页")
    page_size: int = Field(..., description="每页数量")
```

**3. 统一导出** (`app/schemas/__init__.py`):

```python
"""
Schemas统一导出
"""

# 通用Schemas
from app.schemas.common import (
    ErrorResponse,
    ErrorDetail,
    SuccessResponse,
    PaginationParams,
    PaginatedResponse,
    TaskStatus,
)

# 路线图Schemas
from app.schemas.roadmap import (
    RoadmapGenerateRequest,
    RoadmapGenerateResponse,
    RoadmapUpdateRequest,
    RoadmapSummary,
    RoadmapDetail,
    RoadmapListResponse,
    ConceptRetryRequest,
    ConceptRetryResponse,
)

# 教程Schemas
from app.schemas.tutorial import (
    TutorialRetryRequest,
    TutorialRetryResponse,
    TutorialDetail,
)

# ... 其他模块

__all__ = [
    # Common
    "ErrorResponse",
    "ErrorDetail",
    "SuccessResponse",
    "PaginationParams",
    "PaginatedResponse",
    "TaskStatus",
    # Roadmap
    "RoadmapGenerateRequest",
    "RoadmapGenerateResponse",
    "RoadmapUpdateRequest",
    "RoadmapSummary",
    "RoadmapDetail",
    "RoadmapListResponse",
    "ConceptRetryRequest",
    "ConceptRetryResponse",
    # Tutorial
    "TutorialRetryRequest",
    "TutorialRetryResponse",
    "TutorialDetail",
]
```

#### 验收标准
- [ ] 创建10个schema文件，每个文件包含至少3个模型
- [ ] 所有Schema包含中文描述和示例
- [ ] 使用Pydantic v2语法（`model_config`而非`Config`）
- [ ] `__init__.py`正确导出所有Schema
- [ ] 运行`mypy app/schemas/`无类型错误

---

### 任务2: 创建CRUD层并实现BaseCRUD [Day 2]

#### 目标目录结构

```
backend/app/crud/
├── __init__.py
├── base.py               # BaseCRUD泛型类
├── crud_roadmap.py       # 路线图CRUD
├── crud_concept.py       # 概念CRUD
├── crud_tutorial.py      # 教程CRUD
├── crud_resource.py      # 资源推荐CRUD
├── crud_quiz.py          # 测验CRUD
├── crud_task.py          # 任务CRUD
├── crud_user.py          # 用户CRUD
└── crud_progress.py      # 进度CRUD
```

#### BaseCRUD泛型类实现

```python
# app/crud/base.py
"""
BaseCRUD泛型类 - 提供通用的CRUD操作
"""
from typing import Generic, TypeVar, Type, Optional, Any
from sqlalchemy import select, update, delete, func
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.orm import DeclarativeBase
from pydantic import BaseModel

# 定义泛型类型
ModelType = TypeVar("ModelType", bound=DeclarativeBase)
CreateSchemaType = TypeVar("CreateSchemaType", bound=BaseModel)
UpdateSchemaType = TypeVar("UpdateSchemaType", bound=BaseModel)

class BaseCRUD(Generic[ModelType, CreateSchemaType, UpdateSchemaType]):
    """
    基础CRUD类（泛型）
    
    功能：
    - 提供通用的增删改查操作
    - 自动处理分页
    - 支持软删除
    - 类型安全（泛型约束）
    
    使用示例：
    ```python
    class RoadmapCRUD(BaseCRUD[RoadmapMetadata, RoadmapCreate, RoadmapUpdate]):
        pass
    
    crud = RoadmapCRUD(RoadmapMetadata)
    roadmap = await crud.get(session, roadmap_id)
    ```
    """
    
    def __init__(self, model: Type[ModelType]):
        """
        初始化CRUD实例
        
        Args:
            model: SQLModel/SQLAlchemy模型类
        """
        self.model = model
    
    # ===== 基础查询 =====
    
    async def get(
        self, 
        session: AsyncSession, 
        id: Any
    ) -> Optional[ModelType]:
        """
        根据ID获取单条记录
        
        Args:
            session: 数据库会话
            id: 主键ID
            
        Returns:
            模型实例或None
        """
        result = await session.execute(
            select(self.model).where(self.model.id == id)
        )
        return result.scalar_one_or_none()
    
    async def get_multi(
        self,
        session: AsyncSession,
        *,
        skip: int = 0,
        limit: int = 100,
    ) -> list[ModelType]:
        """
        获取多条记录（分页）
        
        Args:
            session: 数据库会话
            skip: 跳过的记录数
            limit: 返回的最大记录数
            
        Returns:
            模型实例列表
        """
        result = await session.execute(
            select(self.model)
            .offset(skip)
            .limit(limit)
        )
        return list(result.scalars().all())
    
    async def count(self, session: AsyncSession) -> int:
        """
        统计记录总数
        
        Args:
            session: 数据库会话
            
        Returns:
            记录总数
        """
        result = await session.execute(
            select(func.count()).select_from(self.model)
        )
        return result.scalar_one()
    
    # ===== 创建操作 =====
    
    async def create(
        self,
        session: AsyncSession,
        *,
        obj_in: CreateSchemaType | dict[str, Any],
    ) -> ModelType:
        """
        创建新记录
        
        Args:
            session: 数据库会话
            obj_in: Pydantic Schema或字典
            
        Returns:
            创建的模型实例
        """
        # 转换为字典
        if isinstance(obj_in, dict):
            create_data = obj_in
        else:
            create_data = obj_in.model_dump(exclude_unset=True)
        
        # 创建实例
        db_obj = self.model(**create_data)
        session.add(db_obj)
        await session.flush()  # 获取自动生成的ID
        await session.refresh(db_obj)
        
        return db_obj
    
    # ===== 更新操作 =====
    
    async def update(
        self,
        session: AsyncSession,
        *,
        db_obj: ModelType,
        obj_in: UpdateSchemaType | dict[str, Any],
    ) -> ModelType:
        """
        更新记录
        
        Args:
            session: 数据库会话
            db_obj: 现有的数据库对象
            obj_in: 更新数据（Pydantic Schema或字典）
            
        Returns:
            更新后的模型实例
        """
        # 转换为字典
        if isinstance(obj_in, dict):
            update_data = obj_in
        else:
            update_data = obj_in.model_dump(exclude_unset=True)
        
        # 更新字段
        for field, value in update_data.items():
            setattr(db_obj, field, value)
        
        session.add(db_obj)
        await session.flush()
        await session.refresh(db_obj)
        
        return db_obj
    
    # ===== 删除操作 =====
    
    async def remove(
        self,
        session: AsyncSession,
        *,
        id: Any,
    ) -> Optional[ModelType]:
        """
        硬删除记录
        
        Args:
            session: 数据库会话
            id: 主键ID
            
        Returns:
            被删除的模型实例或None
        """
        obj = await self.get(session, id)
        if obj:
            await session.delete(obj)
            await session.flush()
        return obj
    
    async def soft_delete(
        self,
        session: AsyncSession,
        *,
        id: Any,
    ) -> Optional[ModelType]:
        """
        软删除记录（设置deleted_at字段）
        
        前提：模型必须有deleted_at字段
        
        Args:
            session: 数据库会话
            id: 主键ID
            
        Returns:
            被软删除的模型实例或None
        """
        from datetime import datetime
        
        obj = await self.get(session, id)
        if obj and hasattr(obj, "deleted_at"):
            obj.deleted_at = datetime.utcnow()
            session.add(obj)
            await session.flush()
            await session.refresh(obj)
        return obj
```

#### 具体CRUD实现示例

```python
# app/crud/crud_roadmap.py
"""
路线图CRUD操作
"""
from typing import Optional
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.orm import selectinload

from app.crud.base import BaseCRUD
from app.models.database import RoadmapMetadata
from app.schemas.roadmap import RoadmapCreate, RoadmapUpdate

class RoadmapCRUD(BaseCRUD[RoadmapMetadata, RoadmapCreate, RoadmapUpdate]):
    """
    路线图CRUD操作
    
    继承BaseCRUD，自动获得：
    - get(id)
    - get_multi(skip, limit)
    - create(obj_in)
    - update(db_obj, obj_in)
    - remove(id)
    - soft_delete(id)
    """
    
    async def get_by_roadmap_id(
        self,
        session: AsyncSession,
        roadmap_id: str,
    ) -> Optional[RoadmapMetadata]:
        """
        根据roadmap_id获取路线图
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            
        Returns:
            路线图元数据或None
        """
        result = await session.execute(
            select(RoadmapMetadata).where(
                RoadmapMetadata.roadmap_id == roadmap_id
            )
        )
        return result.scalar_one_or_none()
    
    async def get_by_user(
        self,
        session: AsyncSession,
        user_id: str,
        *,
        skip: int = 0,
        limit: int = 20,
    ) -> list[RoadmapMetadata]:
        """
        获取用户的路线图列表
        
        Args:
            session: 数据库会话
            user_id: 用户ID
            skip: 跳过的记录数
            limit: 返回的最大记录数
            
        Returns:
            路线图列表
        """
        result = await session.execute(
            select(RoadmapMetadata)
            .where(RoadmapMetadata.user_id == user_id)
            .where(RoadmapMetadata.deleted_at.is_(None))  # 排除软删除
            .order_by(RoadmapMetadata.created_at.desc())
            .offset(skip)
            .limit(limit)
        )
        return list(result.scalars().all())
    
    async def get_with_concepts(
        self,
        session: AsyncSession,
        roadmap_id: str,
    ) -> Optional[RoadmapMetadata]:
        """
        获取路线图及其所有概念元数据（避免N+1查询）
        
        Args:
            session: 数据库会话
            roadmap_id: 路线图ID
            
        Returns:
            路线图元数据（包含关联的concepts）
        """
        result = await session.execute(
            select(RoadmapMetadata)
            .options(selectinload(RoadmapMetadata.concept_metas))
            .where(RoadmapMetadata.roadmap_id == roadmap_id)
        )
        return result.scalar_one_or_none()

# 创建单例（工厂函数）
def get_roadmap_crud() -> RoadmapCRUD:
    """获取RoadmapCRUD实例"""
    return RoadmapCRUD(RoadmapMetadata)
```

#### 验收标准
- [ ] BaseCRUD支持泛型类型约束
- [ ] 包含完整的CRUD操作（create/get/update/delete）
- [ ] 支持软删除
- [ ] 包含分页和计数功能
- [ ] 创建至少5个具体CRUD类
- [ ] 所有方法包含类型注解和文档字符串
- [ ] 通过mypy类型检查

---

### 任务3: 实现Session读写分离 [Day 3]

#### 问题分析

当前`get_db()`自动commit所有操作：

```python
# app/db/session.py (当前代码)
async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with safe_session() as session:
        try:
            yield session
            await session.commit()  # ❌ 读操作也会commit
        except Exception:
            await session.rollback()
            raise
```

**后果**:
- 读操作触发不必要的commit开销
- 事务边界不清晰
- 容易引入意外的数据修改

#### 修复方案

```python
# app/db/session.py (修复后)

from typing import AsyncGenerator, Annotated
from sqlalchemy.ext.asyncio import AsyncSession
from fastapi import Depends
import structlog

logger = structlog.get_logger()

# ===== 只读Session =====

async def get_db_readonly() -> AsyncGenerator[AsyncSession, None]:
    """
    只读Session（不自动commit）
    
    使用场景：
    - GET请求
    - 查询操作
    - 不需要事务的场景
    """
    async with safe_session() as session:
        try:
            yield session
            # ✅ 不commit，只关闭连接
        except Exception as e:
            logger.error("readonly_session_error", error=str(e))
            raise
        finally:
            await session.close()

# ===== 写Session（自动事务管理）=====

async def get_db_transaction() -> AsyncGenerator[AsyncSession, None]:
    """
    写Session（自动事务管理）
    
    使用场景：
    - POST/PUT/DELETE请求
    - 需要修改数据的操作
    - 需要明确事务边界的场景
    
    行为：
    - 成功：自动commit
    - 异常：自动rollback
    """
    async with safe_session() as session:
        try:
            yield session
            await session.commit()  # ✅ 成功时commit
            logger.debug("transaction_committed")
        except Exception as e:
            await session.rollback()  # ✅ 异常时rollback
            logger.error("transaction_rollback", error=str(e))
            raise
        finally:
            await session.close()

# ===== 类型别名（用于依赖注入）=====

# 只读Session
CurrentSession = Annotated[
    AsyncSession,
    Depends(get_db_readonly),
]

# 写Session（事务）
CurrentSessionTransaction = Annotated[
    AsyncSession,
    Depends(get_db_transaction),
]

# ===== 向后兼容（保留get_db，但标记为废弃）=====

async def get_db() -> AsyncGenerator[AsyncSession, None]:
    """
    ⚠️ 已废弃：请使用CurrentSession或CurrentSessionTransaction
    
    保留此函数仅为向后兼容，新代码请勿使用
    """
    import warnings
    warnings.warn(
        "get_db() is deprecated, use CurrentSession or CurrentSessionTransaction",
        DeprecationWarning,
        stacklevel=2,
    )
    async with safe_session() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

#### 使用示例

```python
# app/api/v1/endpoints/roadmaps.py (重构后)

from app.db.session import CurrentSession, CurrentSessionTransaction

# ===== 读操作：使用CurrentSession =====

@router.get("/{roadmap_id}", response_model=RoadmapDetail)
async def get_roadmap(
    roadmap_id: str,
    session: CurrentSession,  # ✅ 只读Session
):
    """获取路线图详情（只读操作）"""
    roadmap_crud = RoadmapCRUD(RoadmapMetadata)
    roadmap = await roadmap_crud.get_by_roadmap_id(session, roadmap_id)
    
    if not roadmap:
        raise HTTPException(status_code=404, detail="Roadmap not found")
    
    return roadmap

# ===== 写操作：使用CurrentSessionTransaction =====

@router.post("/", response_model=RoadmapDetail)
async def create_roadmap(
    request: RoadmapCreateRequest,
    session: CurrentSessionTransaction,  # ✅ 事务Session
):
    """创建路线图（写操作）"""
    roadmap_crud = RoadmapCRUD(RoadmapMetadata)
    
    # 创建路线图
    roadmap = await roadmap_crud.create(
        session,
        obj_in=request,
    )
    
    # ✅ 函数结束时自动commit
    return roadmap

@router.delete("/{roadmap_id}")
async def delete_roadmap(
    roadmap_id: str,
    session: CurrentSessionTransaction,  # ✅ 事务Session
):
    """删除路线图（写操作）"""
    roadmap_crud = RoadmapCRUD(RoadmapMetadata)
    
    # 软删除
    roadmap = await roadmap_crud.soft_delete(session, id=roadmap_id)
    
    if not roadmap:
        raise HTTPException(status_code=404, detail="Roadmap not found")
    
    # ✅ 函数结束时自动commit
    return {"message": "Roadmap deleted successfully"}
```

#### 验收标准
- [ ] 实现`get_db_readonly()`和`get_db_transaction()`
- [ ] 定义`CurrentSession`和`CurrentSessionTransaction`类型别名
- [ ] 编写至少2个使用示例
- [ ] 更新依赖注入文档
- [ ] 保留`get_db()`向后兼容（带废弃警告）

---

### 任务4: 统一依赖注入规范 [Day 4]

#### 目标

创建`app/api/v1/deps.py`作为统一的依赖注入管理文件。

#### 实现

```python
# app/api/v1/deps.py
"""
API依赖注入统一管理
"""
from typing import Annotated
from fastapi import Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_db_readonly, get_db_transaction
from app.crud.crud_roadmap import RoadmapCRUD, get_roadmap_crud
from app.crud.crud_tutorial import TutorialCRUD, get_tutorial_crud
from app.crud.crud_resource import ResourceCRUD, get_resource_crud
from app.crud.crud_quiz import QuizCRUD, get_quiz_crud
from app.crud.crud_user import UserCRUD, get_user_crud
from app.services.roadmap_service import RoadmapService
from app.services.content_service import ContentService
from app.core.auth.deps import get_current_user
from app.models.database import User

# ===== Session依赖 =====

CurrentSession = Annotated[AsyncSession, Depends(get_db_readonly)]
CurrentSessionTransaction = Annotated[AsyncSession, Depends(get_db_transaction)]

# ===== CRUD依赖 =====

CurrentRoadmapCRUD = Annotated[RoadmapCRUD, Depends(get_roadmap_crud)]
CurrentTutorialCRUD = Annotated[TutorialCRUD, Depends(get_tutorial_crud)]
CurrentResourceCRUD = Annotated[ResourceCRUD, Depends(get_resource_crud)]
CurrentQuizCRUD = Annotated[QuizCRUD, Depends(get_quiz_crud)]
CurrentUserCRUD = Annotated[UserCRUD, Depends(get_user_crud)]

# ===== Service依赖 =====

def get_roadmap_service(
    roadmap_crud: CurrentRoadmapCRUD,
    session: CurrentSessionTransaction,
) -> RoadmapService:
    """获取RoadmapService实例"""
    return RoadmapService(roadmap_crud=roadmap_crud, session=session)

CurrentRoadmapService = Annotated[RoadmapService, Depends(get_roadmap_service)]

def get_content_service(
    tutorial_crud: CurrentTutorialCRUD,
    resource_crud: CurrentResourceCRUD,
    quiz_crud: CurrentQuizCRUD,
) -> ContentService:
    """获取ContentService实例"""
    return ContentService(
        tutorial_crud=tutorial_crud,
        resource_crud=resource_crud,
        quiz_crud=quiz_crud,
    )

CurrentContentService = Annotated[ContentService, Depends(get_content_service)]

# ===== 鉴权依赖 =====

CurrentUser = Annotated[User, Depends(get_current_user)]

async def get_current_active_user(current_user: CurrentUser) -> User:
    """获取当前活跃用户（检查账号状态）"""
    if not current_user.is_active:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="Inactive user",
        )
    return current_user

CurrentActiveUser = Annotated[User, Depends(get_current_active_user)]

# ===== 权限检查依赖 =====

class PermissionChecker:
    """权限检查器（可复用）"""
    
    def __init__(self, required_permission: str):
        self.required_permission = required_permission
    
    def __call__(self, current_user: CurrentUser) -> User:
        """检查用户是否有指定权限"""
        if not current_user.has_permission(self.required_permission):
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail=f"Permission denied: {self.required_permission}",
            )
        return current_user

# 使用示例：
# RequireAdmin = Annotated[User, Depends(PermissionChecker("admin"))]
```

#### 使用示例

```python
# app/api/v1/endpoints/roadmaps.py (使用统一依赖注入)

from app.api.v1.deps import (
    CurrentSession,
    CurrentSessionTransaction,
    CurrentRoadmapCRUD,
    CurrentRoadmapService,
    CurrentActiveUser,
)

@router.get("/{roadmap_id}")
async def get_roadmap(
    roadmap_id: str,
    crud: CurrentRoadmapCRUD,  # ✅ 注入CRUD
    session: CurrentSession,    # ✅ 注入只读Session
):
    """获取路线图"""
    return await crud.get_by_roadmap_id(session, roadmap_id)

@router.post("/generate")
async def generate_roadmap(
    request: RoadmapGenerateRequest,
    service: CurrentRoadmapService,  # ✅ 注入Service
    user: CurrentActiveUser,          # ✅ 注入当前用户
):
    """生成路线图"""
    return await service.generate_roadmap(request, user)
```

#### 验收标准
- [ ] 创建`app/api/v1/deps.py`文件
- [ ] 定义所有常用依赖的类型别名
- [ ] 实现权限检查依赖
- [ ] 编写使用示例
- [ ] 更新开发文档

---

### 任务5: 编写开发规范文档 [Day 5]

#### 文档1: Code Review Checklist

```markdown
# backend/docs/CODE_REVIEW_CHECKLIST.md

# Code Review检查清单

## API层（Endpoints）

- [ ] 函数不超过50行
- [ ] 只负责HTTP协议，不包含业务逻辑
- [ ] 使用Pydantic Schema验证输入输出
- [ ] 使用统一的依赖注入（`app/api/v1/deps.py`）
- [ ] 不直接实例化Service或Agent
- [ ] 不直接访问数据库（通过Service/CRUD层）
- [ ] 正确使用`CurrentSession`（读）或`CurrentSessionTransaction`（写）
- [ ] HTTP状态码使用恰当
- [ ] 错误处理规范（HTTPException）
- [ ] 包含中文文档字符串

## Schemas层

- [ ] 所有API输入输出都有对应Schema
- [ ] Schema定义在`app/schemas/`目录
- [ ] 使用`Field`添加描述和验证规则
- [ ] 提供示例（`json_schema_extra`）
- [ ] 避免与Model/Domain对象混用
- [ ] 使用Pydantic v2语法

## Service层

- [ ] 函数职责单一，不超过100行
- [ ] 包含核心业务逻辑
- [ ] 通过CRUD层访问数据库
- [ ] 可单独测试（不依赖HTTP层）
- [ ] 正确处理事务边界
- [ ] 异常处理完善
- [ ] 包含详细的文档字符串

## CRUD层

- [ ] 只包含数据库CRUD操作
- [ ] 不包含业务逻辑
- [ ] 继承`BaseCRUD`
- [ ] 函数命名清晰（`get_by_*`, `create`, `update`, `delete`）
- [ ] 使用`selectinload`避免N+1查询
- [ ] 支持分页和排序
- [ ] 正确处理软删除

## 通用规范

- [ ] 所有函数包含类型注解
- [ ] 所有公开函数包含中文文档字符串
- [ ] 使用`structlog`记录关键操作
- [ ] 敏感信息已脱敏
- [ ] 没有硬编码的配置（使用`settings`）
- [ ] 通过`mypy`类型检查
- [ ] 通过`ruff`代码检查
- [ ] 单元测试覆盖率 >80%
```

#### 文档2: 迁移指南

```markdown
# backend/docs/MIGRATION_GUIDE.md

# 架构迁移指南

## 概述

本指南介绍如何将旧代码迁移到新架构。

## 迁移步骤

### 步骤1: 提取Schemas

**旧代码**（在endpoint文件中定义）:
\`\`\`python
# app/api/v1/endpoints/tutorials.py
class RetryTutorialRequest(BaseModel):
    preferences: dict
\`\`\`

**新代码**（在schemas/目录中）:
\`\`\`python
# app/schemas/tutorial.py
class TutorialRetryRequest(BaseModel):
    preferences: LearningPreferences = Field(...)
    
# app/api/v1/endpoints/tutorials.py
from app.schemas.tutorial import TutorialRetryRequest
\`\`\`

### 步骤2: 创建CRUD层

**旧代码**（在endpoint中直接查询）:
\`\`\`python
@router.get("/{id}")
async def get_tutorial(id: str, session: AsyncSession = Depends(get_db)):
    result = await session.execute(
        select(Tutorial).where(Tutorial.id == id)
    )
    return result.scalar_one_or_none()
\`\`\`

**新代码**（使用CRUD层）:
\`\`\`python
# app/crud/crud_tutorial.py
class TutorialCRUD(BaseCRUD[Tutorial, TutorialCreate, TutorialUpdate]):
    pass

# app/api/v1/endpoints/tutorials.py
@router.get("/{id}")
async def get_tutorial(
    id: str,
    crud: CurrentTutorialCRUD,
    session: CurrentSession,
):
    return await crud.get(session, id)
\`\`\`

### 步骤3: 提取Service层

**旧代码**（业务逻辑在endpoint中）:
\`\`\`python
@router.post("/retry")
async def retry_tutorial(...):
    # 40行业务逻辑
    concept = await get_concept(...)
    agent = TutorialGeneratorAgent()
    result = await agent.execute(...)
    await save_result(...)
\`\`\`

**新代码**（业务逻辑在Service中）:
\`\`\`python
# app/services/content_service.py
class ContentService:
    async def retry_tutorial(self, concept_id: str, ...):
        # 业务逻辑

# app/api/v1/endpoints/tutorials.py
@router.post("/retry")
async def retry_tutorial(
    request: TutorialRetryRequest,
    service: CurrentContentService,
):
    return await service.retry_tutorial(request.concept_id, ...)
\`\`\`

## 常见问题

### Q: 旧代码何时删除？
A: 迁移完成并测试通过后，立即删除旧代码。

### Q: 如何处理复杂的Repository？
A: 拆分为多个CRUD类，按实体划分。

### Q: Service之间如何调用？
A: 通过依赖注入，避免循环依赖。
```

#### 验收标准
- [ ] 编写Code Review Checklist
- [ ] 编写迁移指南
- [ ] 编写依赖注入最佳实践文档
- [ ] 所有文档包含中文说明和代码示例
- [ ] 团队成员已review并达成共识

---

## 🔍 验收标准

### 目录结构验收

```bash
# 检查目录结构
tree app/schemas/ app/crud/
```

预期输出：
```
app/schemas/
├── __init__.py
├── common.py
├── roadmap.py
├── tutorial.py
├── resource.py
├── quiz.py
├── mentor.py
├── user.py
├── progress.py
└── tech_assessment.py

app/crud/
├── __init__.py
├── base.py
├── crud_roadmap.py
├── crud_tutorial.py
├── crud_resource.py
├── crud_quiz.py
├── crud_task.py
├── crud_user.py
└── crud_progress.py
```

### 代码质量验收

```bash
# 类型检查
mypy app/schemas/ app/crud/ --strict

# 代码风格检查
ruff check app/schemas/ app/crud/

# 运行测试
pytest tests/unit/test_base_crud.py -v
```

### 文档完整性验收

- [ ] 所有Schema包含中文描述
- [ ] 所有CRUD方法包含文档字符串
- [ ] BaseCRUD包含完整的使用示例
- [ ] 编写了Code Review Checklist
- [ ] 编写了迁移指南

---

## ⚠️ 风险控制

### 风险1: 新旧代码共存混乱

**缓解措施**:
- 明确命名规范：新代码必须使用`app/schemas/`和`app/crud/`
- Code Review强制执行
- 定期清理未使用的import

### 风险2: 泛型类型使用不当

**缓解措施**:
- 提供详细的使用示例
- 编写单元测试覆盖所有泛型场景
- 使用mypy严格模式检查

### 风险3: 团队学习成本

**缓解措施**:
- 组织内部培训会议
- 提供详细的迁移指南
- 结对编程推广新模式

---

## 🧪 测试要求

### BaseCRUD单元测试

```python
# tests/unit/test_base_crud.py

import pytest
from app.crud.base import BaseCRUD
from app.models.database import RoadmapMetadata

@pytest.mark.asyncio
async def test_base_crud_get(session):
    """测试：BaseCRUD.get()"""
    crud = BaseCRUD(RoadmapMetadata)
    
    # 创建测试数据
    obj = RoadmapMetadata(roadmap_id="test-123", user_id="user-1")
    session.add(obj)
    await session.commit()
    
    # 测试get
    result = await crud.get(session, obj.id)
    assert result is not None
    assert result.roadmap_id == "test-123"

@pytest.mark.asyncio
async def test_base_crud_create(session):
    """测试：BaseCRUD.create()"""
    crud = BaseCRUD(RoadmapMetadata)
    
    obj_in = {"roadmap_id": "test-456", "user_id": "user-2"}
    result = await crud.create(session, obj_in=obj_in)
    
    assert result.id is not None
    assert result.roadmap_id == "test-456"

@pytest.mark.asyncio
async def test_base_crud_update(session):
    """测试：BaseCRUD.update()"""
    crud = BaseCRUD(RoadmapMetadata)
    
    # 创建
    obj = await crud.create(session, obj_in={
        "roadmap_id": "test-789",
        "user_id": "user-3",
        "title": "Old Title",
    })
    
    # 更新
    updated = await crud.update(session, db_obj=obj, obj_in={
        "title": "New Title",
    })
    
    assert updated.title == "New Title"
    assert updated.roadmap_id == "test-789"  # 未修改字段保持不变
```

---

## ⏱️ 时间估算

| 任务 | 开发 | 测试 | 文档 | 总计 |
|------|------|------|------|------|
| Schemas目录 | 4h | 1h | 1h | 6h |
| BaseCRUD | 4h | 2h | 1h | 7h |
| Session分离 | 2h | 1h | 1h | 4h |
| 依赖注入 | 2h | 1h | 1h | 4h |
| 开发规范文档 | - | - | 4h | 4h |
| **总计** | **12h** | **5h** | **8h** | **25h** |

**建议工期**: 5个工作日（按每天5小时有效工作时间计算）

---

## 📊 依赖关系

### 前置条件

- ✅ 阶段0已完成（紧急止血）
- ✅ 系统稳定运行
- ✅ 团队已达成共识

### 后续阶段

完成本阶段后，可以进入：
- **阶段2**: 核心模块重构（依赖schemas/和crud/）
- **阶段3**: 全面架构迁移

### 并行任务

以下任务可以并行开发：
- Schemas目录 ‖ CRUD目录（独立创建）
- Session分离 ‖ 依赖注入（独立功能）

---

## 📝 文档更新

完成后需更新：

1. **架构文档**: 更新目录结构说明
2. **开发指南**: 新增schemas/和crud/使用说明
3. **API文档**: 使用新Schema自动生成文档

---

## ✅ 完成检查清单

- [ ] `app/schemas/`目录包含10个schema文件
- [ ] `app/crud/`目录包含BaseCRUD和8个CRUD类
- [ ] Session读写分离实现完成
- [ ] `app/api/v1/deps.py`统一依赖注入
- [ ] Code Review Checklist编写完成
- [ ] 迁移指南编写完成
- [ ] 所有代码通过mypy类型检查
- [ ] BaseCRUD单元测试覆盖率>90%
- [ ] 团队培训会议已完成
- [ ] 文档已review并合并

---

**文档版本**: v1.0  
**创建日期**: 2026-01-05  
**预计完成**: 2026-01-12  
**负责人**: 后端架构组  
**审核人**: 技术负责人

