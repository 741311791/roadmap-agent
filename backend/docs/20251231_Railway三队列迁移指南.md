# Railway 三队列架构迁移指南

**迁移日期**: 2024-12-31  
**从**: 两队列（logs + content_generation）  
**到**: 三队列（logs + content_generation + roadmap_workflow）

---

## 迁移原因

### 为什么需要新增 `roadmap_workflow` 队列？

#### 问题背景

原先的架构中，路线图生成任务（`generate_roadmap`）和工作流恢复任务（`resume_after_review`、`resume_from_checkpoint`）都混在 `default` 队列或 `content_generation` 队列中。

**存在的问题**:

1. **资源竞争**: 长时间运行的工作流任务（可能持续 30-60 分钟）阻塞内容生成任务
2. **超时设置冲突**: 
   - 内容生成：30 分钟足够
   - 完整工作流：需要 1 小时
3. **Checkpoint 隔离不足**: LangGraph checkpoint 在同一队列中可能混淆
4. **监控困难**: 无法区分工作流任务和内容生成任务的性能指标

#### 新架构优势

```
旧架构:
┌─────────────────────────────────┐
│   content_generation 队列        │
│  ├─ generate_roadmap (60 分钟)  │ ← 阻塞其他任务
│  ├─ generate_roadmap_content    │
│  ├─ resume_after_review         │
│  └─ retry_failed_concepts       │
└─────────────────────────────────┘

新架构:
┌──────────────────────────────────┐
│   roadmap_workflow 队列 (新增)   │
│  ├─ generate_roadmap             │ ← 专用队列
│  ├─ resume_after_review          │
│  └─ resume_from_checkpoint       │
└──────────────────────────────────┘
┌──────────────────────────────────┐
│   content_generation 队列         │
│  ├─ generate_roadmap_content     │ ← 不受工作流阻塞
│  ├─ retry_failed_concepts        │
│  └─ retry_failed_resources       │
└──────────────────────────────────┘
```

---

## 迁移步骤

### 步骤 1: 更新代码仓库

```bash
# 拉取最新代码
git pull origin main

# 确认包含以下文件更新
git log --oneline --name-only | grep -E "(railway_entrypoint.sh|celery_app.py)"
```

**需要确认的文件**:
- ✅ `backend/scripts/railway_entrypoint.sh` - 新增 `celery_workflow` 服务类型
- ✅ `backend/app/core/celery_app.py` - 新增队列路由规则

### 步骤 2: 在 Railway 创建新服务

#### 2.1 克隆现有 Celery 服务

1. 打开 Railway Dashboard
2. 进入项目：`roadmap-agent`
3. 点击 "New Service"
4. 选择 "From Existing Service"
5. 选择 `roadmap-celery-content` 作为模板

#### 2.2 配置新服务

**服务名称**: `roadmap-celery-workflow`

**环境变量**（新增/修改）:
```bash
# 必须修改
SERVICE_TYPE=celery_workflow

# 可选优化（推荐）
CELERY_WORKFLOW_CONCURRENCY=4
CELERY_LOG_LEVEL=info
```

**保留的环境变量**（从模板继承）:
```bash
DATABASE_URL=...
REDIS_URL=...
OPENAI_API_KEY=...
TAVILY_API_KEY=...
JWT_SECRET_KEY=...
```

#### 2.3 资源配置

| 配置项 | 推荐值 | 说明 |
|-------|-------|------|
| Memory | 512MB | 标准生产环境 |
| CPU | Shared | 共享 CPU 即可 |
| Start Command | 默认（使用 `railway_entrypoint.sh`） | |

### 步骤 3: 更新现有服务环境变量

#### 3.1 更新 `roadmap-celery-logs`

```bash
# 新增/调整（可选）
CELERY_LOGS_CONCURRENCY=4  # 从 2 提升到 4
```

#### 3.2 更新 `roadmap-celery-content`

```bash
# 新增/调整（可选）
CELERY_CONTENT_CONCURRENCY=6  # 从 2 提升到 6
```

### 步骤 4: 部署验证

#### 4.1 检查服务启动

在 Railway Dashboard 中：

```bash
# roadmap-celery-workflow 服务日志
🔄 Starting Celery Worker for Roadmap Workflow Queue...
[INFO] Connected to rediss://...
[INFO] celery@workflow ready.
```

**关键检查点**:
- ✅ 服务状态显示为 "Running"
- ✅ 日志中无 Redis/PostgreSQL 连接错误
- ✅ 显示队列名称为 `roadmap_workflow`

#### 4.2 测试任务路由

触发一个路线图生成任务：

```bash
# 方法 1: 通过前端 UI
访问: https://your-app.railway.app
创建新路线图 → 观察日志

# 方法 2: 通过 API 测试
curl -X POST "https://your-api.railway.app/api/v1/generation/roadmap" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"query": "Learn Python", ...}'
```

**预期行为**:

1. **roadmap-celery-workflow 日志**:
```
[INFO] Task app.tasks.roadmap_generation_tasks.generate_roadmap[...] received
[INFO] intent_analysis_started
[INFO] curriculum_design_started
...
```

2. **roadmap-celery-content 日志**:
```
[INFO] Task app.tasks.content_generation_tasks.generate_roadmap_content[...] received
[INFO] concept_start concept_id=xxx
...
```

**关键验证**:
- ✅ 工作流任务（Intent、Curriculum、Validation、Review）在 `workflow` 队列
- ✅ 内容生成任务（Tutorial、Resources、Quiz）在 `content_generation` 队列
- ✅ 两个队列互不阻塞

---

## 回滚计划

### 如果迁移出现问题

#### 方案 1: 快速回滚（暂停新服务）

1. 在 Railway Dashboard 中暂停 `roadmap-celery-workflow` 服务
2. 工作流任务会自动重新路由到 `default` 队列（由 `celery_content` 处理）

**注意**: 这会恢复原有的资源竞争问题，但不会影响功能。

#### 方案 2: 回滚代码

```bash
# 回滚到迁移前的 commit
git revert <migration-commit-hash>
git push origin main

# Railway 会自动重新部署
```

#### 方案 3: 临时配置（紧急情况）

如果无法快速回滚，可以临时让 `celery_workflow` 服务也处理 `content_generation` 队列：

```bash
# 在 roadmap-celery-workflow 服务中修改环境变量
# (仅作为临时应急措施)
SERVICE_TYPE=celery_content  # 临时改为 content
```

---

## 性能对比

### 迁移前（两队列）

```
测试场景：同时生成 3 个路线图

时间线:
0:00 - Roadmap 1 开始工作流（占用 Worker 1）
0:01 - Roadmap 2 开始工作流（占用 Worker 2）
0:05 - Roadmap 1 进入内容生成（占用 Worker 1）
0:10 - Roadmap 2 进入内容生成（阻塞！Worker 全部被占用）
1:00 - Roadmap 1 完成（释放 Worker 1）
1:01 - Roadmap 2 内容生成开始（延迟 51 分钟）❌

总耗时: ~2 小时
```

### 迁移后（三队列）

```
测试场景：同时生成 3 个路线图

时间线:
0:00 - Roadmap 1 开始工作流（workflow 队列 Worker 1）
0:01 - Roadmap 2 开始工作流（workflow 队列 Worker 2）
0:02 - Roadmap 3 开始工作流（workflow 队列 Worker 3）
0:05 - Roadmap 1 进入内容生成（content 队列 Worker 1）✅
0:06 - Roadmap 2 进入内容生成（content 队列 Worker 2）✅
0:07 - Roadmap 3 进入内容生成（content 队列 Worker 3）✅
0:30 - Roadmap 1 完成
0:32 - Roadmap 2 完成
0:34 - Roadmap 3 完成

总耗时: ~35 分钟
```

**性能提升**: 约 **3.4 倍**（从 2 小时降至 35 分钟）

---

## 监控要点

### 迁移后需要观察的指标

#### 1. 队列长度

使用 Flower 或 Celery CLI 查看：

```bash
# 连接到 Railway 服务
railway run celery -A app.core.celery_app inspect reserved

# 预期输出
{
  "logs@hostname": [],         # ✅ 通常为空（快速处理）
  "content@hostname": [2 tasks],  # ✅ 中等负载
  "workflow@hostname": [1 task],  # ✅ 低负载（长时间任务）
}
```

#### 2. 任务成功率

```bash
# 查看过去 1 小时的任务统计
celery -A app.core.celery_app inspect stats

# 关键指标
{
  "total": 150,
  "roadmap_workflow": {
    "succeeded": 45,
    "failed": 0,      # ✅ 应该为 0
    "retries": 0
  },
  "content_generation": {
    "succeeded": 100,
    "failed": 3,      # ✅ < 5% 失败率可接受
    "retries": 2
  }
}
```

#### 3. Worker 健康状态

```bash
# 检查 Worker 是否响应
celery -A app.core.celery_app inspect ping

# 预期输出
{
  "logs@hostname": {"ok": "pong"},
  "content@hostname": {"ok": "pong"},
  "workflow@hostname": {"ok": "pong"}  # ✅ 新增 Worker
}
```

---

## 故障排查

### 问题 1: 新服务启动失败

**症状**: `roadmap-celery-workflow` 服务无法启动

**排查步骤**:
```bash
# 1. 检查环境变量
Railway Dashboard → roadmap-celery-workflow → Variables
确认: SERVICE_TYPE=celery_workflow

# 2. 查看启动日志
Railway Dashboard → roadmap-celery-workflow → Logs
搜索: "ERROR" 或 "FAIL"

# 3. 常见错误及解决
错误: "Unknown SERVICE_TYPE: celery_workflow"
→ 代码未更新，执行 git pull

错误: "Cannot connect to Redis"
→ 检查 REDIS_URL 环境变量

错误: "Cannot connect to PostgreSQL"
→ 检查 DATABASE_URL 环境变量
```

### 问题 2: 任务未路由到新队列

**症状**: 工作流任务仍然在 `content_generation` 队列执行

**排查步骤**:
```bash
# 1. 检查任务路由配置
# 文件: backend/app/core/celery_app.py
task_routes={
    "roadmap_generation.*": {"queue": "roadmap_workflow"},  # 确认存在
    "workflow_resume.*": {"queue": "roadmap_workflow"},     # 确认存在
}

# 2. 重启所有 Celery 服务
Railway Dashboard → 每个 Celery 服务 → "Restart"

# 3. 清理 Redis 队列（慎用，会丢失未处理任务）
redis-cli -u $REDIS_URL
> DEL celery  # 清空所有队列
```

### 问题 3: 性能未提升

**症状**: 迁移后任务完成时间没有明显改善

**可能原因**:
1. 并发数设置太低
2. 其他瓶颈（数据库、LLM API）
3. 任务本身的逻辑问题

**解决方案**:
```bash
# 1. 提高并发数
CELERY_WORKFLOW_CONCURRENCY=6  # 从 4 提升到 6
CELERY_CONTENT_CONCURRENCY=8   # 从 6 提升到 8

# 2. 检查数据库连接池
DATABASE_POOL_SIZE=30          # 从 20 提升到 30

# 3. 监控 LLM API 响应时间
# 查看日志中的 LLM 调用耗时
grep "llm_call_completed" logs | awk '{print $5}'
```

---

## 成本影响

### Railway 账单变化

| 项目 | 迁移前 | 迁移后 | 变化 |
|-----|--------|--------|------|
| 服务数量 | 3 个 | 4 个 | +1 |
| 总内存 | 1.5GB | 2GB | +512MB |
| 预估月费 | ~$15 | ~$20 | +33% |

**优化建议**:
- 开发环境：可以关闭 `workflow` 服务，使用 `content` 队列处理所有任务
- 低峰期：降低并发数或暂停部分服务

---

## 检查清单

### 迁移前

- [ ] 备份当前 Railway 配置（导出环境变量）
- [ ] 确认 git 仓库已更新到最新版本
- [ ] 在本地测试新脚本（`bash -n railway_entrypoint.sh`）
- [ ] 通知团队计划的迁移时间窗口

### 迁移中

- [ ] 创建新的 `roadmap-celery-workflow` 服务
- [ ] 配置环境变量（`SERVICE_TYPE=celery_workflow`）
- [ ] 等待服务启动成功（状态为 "Running"）
- [ ] 触发测试任务，验证路由正确

### 迁移后

- [ ] 所有服务状态为 "Running"
- [ ] 工作流任务在 `workflow` 队列执行
- [ ] 内容生成任务在 `content_generation` 队列执行
- [ ] 日志任务在 `logs` 队列执行
- [ ] 监控 24 小时，确认无异常

---

## 相关文档

- [Railway 多队列部署配置](./20251231_Railway多队列部署配置.md)
- [Celery 任务优化与速率限制](./20251231_Celery任务优化与速率限制.md)
- [并发控制机制分析](./20251231_并发控制机制分析.md)

