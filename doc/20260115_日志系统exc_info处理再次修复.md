# Celery日志系统exc_info处理修复

**日期**: 2026-01-15  
**类型**: Critical Bug 修复

## 📋 原子事实清单 (Atomic Facts)

### 错误表现
```
TypeError: cannot unpack non-iterable bool object
位置: app/utils/log_formatters.py:114 (实际为123行的 unpack 操作)
触发场景: Celery任务执行失败时的异常日志记录
```

### 异常级联链
```
Level 1: 阿里云LLM API错误 (data_inspection_failed)
    ↓
Level 2: logger.error(..., exc_info=True) 调用
    ↓
Level 3: structlog 处理器处理 exc_info
    ↓
Level 4: format_exception_compact(True) ← 🔴 TypeError
    ↓
Level 5: 级联失败（3次重复）
```

---

# 🧪 物理/逻辑公理验证

## 修复前的断裂链条

```python
# 1. Logger调用（业务代码）
logger.error("error_message", exc_info=True)

# 2. Structlog 处理器（logging_config.py）
exc_info = event_dict.pop("exc_info", None)  # → True (bool)
if exc_info and exc_info != (None, None, None):  # ✅ 通过检查
    event_dict["exception"] = format_exception_compact(exc_info)  # ❌ 传入 bool

# 3. 格式化函数（log_formatters.py）
def format_exception_compact(exc_info: tuple):
    # ... 在某些情况下跳过 bool 处理逻辑
    exc_type, exc_value, exc_traceback = exc_info  # 💥 TypeError!
```

### 修复后的逻辑流

```python
logger.error(..., exc_info=True)
    ↓
format_exc_info_compact(event_dict)
    ↓
if isinstance(exc_info, bool) and exc_info:  # ✅ 在处理器层转换
    exc_info = sys.exc_info()
    ↓
format_exception_compact(exc_info)  # 现在保证接收到三元组
    ↓
正常处理 ✅
```

---

## 修复验证清单

- [x] 在 `logging_config.py` 中提前转换 `exc_info=True`
- [x] 添加 try-except 保护日志处理器
- [x] 简化 `format_exception_compact` 函数逻辑
- [x] 添加类型验证和防御性检查

---

## 测试验证

现在重启 Celery Worker 和 FastAPI 服务，重新运行测试：

```bash
# 重启服务以加载新代码
cd backend
make dev  # 或使用你的启动命令
```

日志处理器现在会：
1. ✅ 在 `format_exc_info_compact` 中提前转换 `exc_info=True`
2. ✅ 使用 try-except 保护格式化逻辑
3. ✅ 在 `format_exception_compact` 中验证输入类型

这样即使再次遇到异常，日志系统也不会崩溃，能正确记录原始错误。🛡️
