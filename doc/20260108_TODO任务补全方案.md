# TODO 任务补全方案

> **生成时间**：2026-01-08  
> **状态**：待执行  
> **覆盖范围**：12 个 TODO 项（排除功能增强类任务）

---

## 📋 任务优先级分类

### 🔴 高优先级（立即处理）
1. 重试/重新生成逻辑（4处）
2. 修改功能逻辑（2处）
3. 权限控制（1处）

### 🟡 中优先级（本周处理）
4. 统计数据完善（2处）
5. 邮件通知（1处）

### 🟢 低优先级（下个迭代）
6. 架构重构（1处）
7. 数据清理逻辑（1处）
8. 类型自动生成（1处）

---

## 🔴 高优先级任务

### 1. generation.py 重试逻辑（行343）

**文件**：`backend/app/api/v1/endpoints/generation.py`  
**TODO 位置**：Line 343

**现状分析**：
- 当前直接返回 `{"message": "重试功能待实现"}`
- 该端点用于路线图生成失败后的重试

**补全方案**：

```python
# TODO: 实现重试逻辑
# 建议使用 retry.py 中的统一重试接口
return response_base.redirect(
    message="请使用统一的重试接口",
    redirect_to=f"/api/v1/roadmaps/{roadmap_id}/retry-failed"
)
```

**或者删除此端点**：
- 因为 `retry.py` 已经提供了完整的重试功能
- 建议在 `router.py` 中移除此端点的注册
- 更新前端调用为使用 `/api/v1/roadmaps/{roadmap_id}/retry-failed`

**推荐**：删除此端点，统一使用 `retry.py` 的接口。

---

### 2. retry.py 概念内容重新生成逻辑（行145）

**文件**：`backend/app/api/v1/endpoints/retry.py`  
**TODO 位置**：Line 145

**现状分析**：
- 端点 `/roadmaps/{roadmap_id}/concepts/{concept_id}/regenerate`
- 用于重新生成指定概念的所有内容（教程+资源+测验）
- 当前返回占位响应

**补全方案**：

```python
# 在 TODO 位置替换为：

# 调用 ContentService 重新生成所有内容
from app.services.content_service import get_content_service
content_service = get_content_service()

result = {
    "success": True,
    "concept_id": concept_id,
    "regenerated_items": []
}

# 依次生成三种内容
for content_type in ["tutorial", "resources", "quiz"]:
    try:
        retry_request = ConceptRetryRequest(
            user_id=request.preferences.user_id if hasattr(request.preferences, 'user_id') else 'system',
            concept_id=concept_id,
            content_types=[content_type],
            preferences=request.preferences,
        )
        
        retry_result = await content_service.retry_concept_content(
            session=session,
            roadmap_id=roadmap_id,
            request=retry_request,
        )
        
        result["regenerated_items"].append({
            "content_type": content_type,
            "status": "completed",
            "details": retry_result
        })
        
    except Exception as e:
        logger.error(
            "regenerate_content_failed",
            content_type=content_type,
            error=str(e)
        )
        result["regenerated_items"].append({
            "content_type": content_type,
            "status": "failed",
            "error": str(e)
        })

# 提交数据库事务
await session.commit()

return result
```

**依赖**：需要导入 `ConceptRetryRequest` schema。

---

### 3. retry_service_new.py 重新生成逻辑（行125）

**文件**：`backend/app/services/retry_service_new.py`  
**TODO 位置**：Line 125

**现状分析**：
- `prepare_regenerate_content` 方法只做了基础验证
- 需要补全实际的重新生成准备逻辑

**补全方案**：

```python
# 在 TODO 位置替换为：

# 检查概念元数据是否存在
concept_metadata = await self.concept_crud.get_by_concept_id(session, concept_id)
if not concept_metadata:
    # 尝试从 roadmap framework_data 中获取
    framework_data = roadmap_metadata.framework_data or {}
    concept_data = None
    
    for stage in framework_data.get("stages", []):
        for module in stage.get("modules", []):
            for concept in module.get("concepts", []):
                if concept.get("concept_id") == concept_id:
                    concept_data = concept
                    break
            if concept_data:
                break
        if concept_data:
            break
    
    if not concept_data:
        raise ValueError(f"概念 {concept_id} 不存在")
else:
    concept_data = {
        "concept_id": concept_metadata.concept_id,
        "name": concept_metadata.name,
        "description": concept_metadata.description,
        "estimated_hours": concept_metadata.estimated_hours,
        "prerequisites": concept_metadata.prerequisites or [],
        "difficulty": concept_metadata.difficulty,
        "keywords": concept_metadata.keywords or [],
    }

# 返回准备好的数据
return {
    "roadmap_id": roadmap_id,
    "concept_id": concept_id,
    "concept_data": concept_data,
    "roadmap_title": roadmap_metadata.title,
    "topic": roadmap_metadata.topic,
}
```

---

### 4. modification.py 资源修改逻辑（行124）

**文件**：`backend/app/api/v1/endpoints/modification.py`  
**TODO 位置**：Line 124

**现状分析**：
- `modify_resources` 端点未实现
- `modify_tutorial` 已有完整实现可参考

**补全方案**：

```python
# 在 TODO 位置替换为：

logger.info(
    "modify_resources_requested",
    roadmap_id=roadmap_id,
    concept_id=concept_id,
    requirements_count=len(request.requirements),
)

try:
    result = await service.modify_resources(
        session=session,
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        requirements=request.requirements,
        preferences=request.preferences,
    )
    await session.commit()
    
    return result
    
except ValueError as e:
    raise HTTPException(status_code=404, detail=str(e))
except Exception as e:
    logger.error(
        "modify_resources_failed",
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        error=str(e),
    )
    raise HTTPException(status_code=500, detail=f"资源修改失败: {str(e)}")
```

**同时需要在 `content_service.py` 中添加 `modify_resources` 方法**：

```python
async def modify_resources(
    self,
    session: AsyncSession,
    roadmap_id: str,
    concept_id: str,
    requirements: list[str],
    preferences: LearningPreferences,
) -> dict:
    """
    修改指定概念的资源推荐
    
    Args:
        session: 数据库会话
        roadmap_id: 路线图 ID
        concept_id: 概念 ID
        requirements: 修改要求列表
        preferences: 用户学习偏好
        
    Returns:
        修改结果字典
        
    Raises:
        ValueError: 路线图、概念或资源不存在
    """
    # 获取现有资源
    resources = await self.resource_crud.get_by_concept_id(session, concept_id)
    if not resources:
        raise ValueError(f"概念 {concept_id} 的资源推荐不存在")
    
    # 获取概念数据
    concept_metadata = await self.concept_crud.get_by_concept_id(session, concept_id)
    if not concept_metadata:
        raise ValueError(f"概念 {concept_id} 不存在")
    
    concept_data = {
        "concept_id": concept_metadata.concept_id,
        "name": concept_metadata.name,
        "description": concept_metadata.description,
        "keywords": concept_metadata.keywords or [],
    }
    
    # 构建 Concept 对象
    from app.models.domain import Concept
    concept = Concept(
        concept_id=concept_data["concept_id"],
        name=concept_data["name"],
        description=concept_data["description"],
        estimated_hours=concept_metadata.estimated_hours,
        prerequisites=concept_metadata.prerequisites or [],
        difficulty=concept_metadata.difficulty,
        keywords=concept_data["keywords"],
    )
    
    # 获取上下文
    roadmap = await self.roadmap_crud.get_by_roadmap_id(session, roadmap_id)
    context = {
        "roadmap_topic": roadmap.topic,
        "roadmap_title": roadmap.title,
    }
    
    # 添加版本信息到上下文
    context["content_version"] = resources.content_version
    
    # 调用修改Agent
    from app.agents.resource_modifier import ResourceModifierAgent
    from app.models.domain import ResourceModificationInput
    
    modifier = ResourceModifierAgent()
    
    modification_input = ResourceModificationInput(
        concept=concept,
        context=context,
        user_preferences=preferences,
        existing_content_url=resources.content_url,
        modification_requirements=requirements,
    )
    
    result = await modifier.modify(modification_input)
    
    # 保存新版本到数据库
    from app.models.domain import ResourceRecommendationOutput
    resource_output = ResourceRecommendationOutput(
        concept_id=result.concept_id,
        resources_id=result.resources_id,
        resources=result.resources,
        content_url=result.content_url,
        content_status="completed",
        content_version=result.content_version,
        generated_at=result.generated_at,
    )
    
    # 使用concept_service保存
    await self.concept_service.save_resources_metadata(
        session, resource_output, roadmap_id
    )
    
    return {
        "success": True,
        "concept_id": result.concept_id,
        "resources_id": result.resources_id,
        "resources_count": len(result.resources),
        "content_url": result.content_url,
        "content_version": result.content_version,
        "modification_summary": result.modification_summary,
        "changes_made": result.changes_made,
    }
```

---

### 5. modification.py 测验修改逻辑（行163）

**文件**：`backend/app/api/v1/endpoints/modification.py`  
**TODO 位置**：Line 163

**补全方案**：与资源修改类似，在端点中调用 Service：

```python
# 在 TODO 位置替换为：

try:
    result = await service.modify_quiz(
        session=session,
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        requirements=request.requirements,
        preferences=request.preferences,
    )
    await session.commit()
    
    return result
    
except ValueError as e:
    raise HTTPException(status_code=404, detail=str(e))
except Exception as e:
    logger.error(
        "modify_quiz_failed",
        roadmap_id=roadmap_id,
        concept_id=concept_id,
        error=str(e),
    )
    raise HTTPException(status_code=500, detail=f"测验修改失败: {str(e)}")
```

**同时在 `content_service.py` 中添加 `modify_quiz` 方法**（参考 `modify_resources` 的实现模式）。

---

### 6. auth_ext.py 管理员权限检查（行173）

**文件**：`backend/app/api/v1/endpoints/auth_ext.py`  
**TODO 位置**：Line 173

**现状分析**：
- 代码已经注释好，只需取消注释并验证

**补全方案**：

```python
# 在 TODO 位置替换为：

# 检查管理员权限
if not current_user.is_superuser:
    raise errors.ForbiddenError(msg="需要管理员权限")

stats = await get_blacklist_stats()

return response_base.success(data=BlacklistStatsResponse(
    total_tokens=stats["total_tokens"],
    sample_tokens=stats["sample_tokens"],
))
```

**验证点**：
- 确保 `current_user` 对象有 `is_superuser` 字段
- 确保 `errors.ForbiddenError` 已正确导入

---

## 🟡 中优先级任务

### 7. user_service.py 计算 in_progress 任务数（行243）

**文件**：`backend/app/services/user_service.py`  
**TODO 位置**：Line 243

**现状分析**：
- `get_roadmap_history` 方法返回的 `in_progress_count` 固定为 0

**补全方案**：

```python
# 在构造 RoadmapHistoryResponse 之前添加：

# 统计进行中的任务数量
from app.models.database import RoadmapTask
in_progress_result = await session.execute(
    select(func.count(RoadmapTask.task_id))
    .where(
        RoadmapTask.user_id == user_id,
        RoadmapTask.status.in_(["pending", "processing"])
    )
)
in_progress_count = in_progress_result.scalar() or 0

# 然后在返回时使用：
return RoadmapHistoryResponse(
    roadmaps=result_list,
    total=len(roadmaps),
    in_progress_count=in_progress_count,
)
```

---

### 8. user_service.py 实际统计各状态任务数量（行380）

**文件**：`backend/app/services/user_service.py`  
**TODO 位置**：Line 380

**现状分析**：
- `get_user_tasks` 方法返回的各状态计数固定为 0

**补全方案**：

```python
# 在 result_list 构造完成后添加：

# 统计各状态的任务数量
from sqlalchemy import func
status_stats_result = await session.execute(
    select(
        RoadmapTask.status,
        func.count(RoadmapTask.task_id).label("count")
    )
    .where(RoadmapTask.user_id == user_id)
    .group_by(RoadmapTask.status)
)
status_stats = {row.status: row.count for row in status_stats_result.fetchall()}

# 获取各状态计数
pending_count = status_stats.get("pending", 0)
processing_count = status_stats.get("processing", 0)
completed_count = status_stats.get("completed", 0)
failed_count = status_stats.get("failed", 0)

# 返回时使用实际统计值
return TaskListResponse(
    tasks=result_list,
    total=len(tasks),
    pending_count=pending_count,
    processing_count=processing_count,
    completed_count=completed_count,
    failed_count=failed_count,
)
```

---

### 9. user_manager.py 发送重置密码邮件（行108）

**文件**：`backend/app/core/auth/user_manager.py`  
**TODO 位置**：Line 108

**现状分析**：
- MVP 阶段可能不需要邮件功能
- 建议使用第三方邮件服务（如 Resend, SendGrid, AWS SES）

**补全方案（选项 A：使用 Resend）**：

```python
# TODO: 发送重置密码邮件
from app.core.email import send_password_reset_email
from app.config.settings import settings

if settings.ENABLE_EMAIL_NOTIFICATIONS:
    try:
        await send_password_reset_email(
            user_email=user.email,
            user_name=user.email.split('@')[0],  # 或使用真实用户名
            reset_token=token,
        )
        logger.info(
            "password_reset_email_sent",
            user_id=user.id,
            email=user.email,
        )
    except Exception as e:
        logger.error(
            "password_reset_email_failed",
            user_id=user.id,
            email=user.email,
            error=str(e),
        )
else:
    logger.info(
        "password_reset_email_skipped",
        user_id=user.id,
        reason="Email notifications disabled"
    )
```

**补全方案（选项 B：MVP 阶段保持占位）**：

```python
# TODO: 发送重置密码邮件
# MVP 阶段暂不实现邮件功能
# 建议在 Phase 2 使用 Resend 或 SendGrid 实现
logger.info(
    "password_reset_requested",
    user_id=user.id,
    email=user.email,
    note="Email sending not implemented in MVP"
)
```

**推荐**：选项 B（MVP 阶段保持占位）。

---

## 🟢 低优先级任务

### 10. roadmap_service.py Phase 2 架构重构

**文件**：`backend/app/services/roadmap_service.py`  
**TODO 位置**：Line 18, 28, 35

**现状分析**：
- 当前使用 Repository 工厂模式
- Phase 2 计划重构为直接使用 CRUD + AsyncSession 注入

**补全方案**：

**第一步：创建新的服务接口**

```python
# 新建 roadmap_service_v2.py

class RoadmapServiceV2:
    """
    路线图生成服务（重构版）
    
    直接使用 CRUD + AsyncSession 依赖注入
    """
    
    def __init__(
        self,
        roadmap_crud: Optional[RoadmapCRUD] = None,
        concept_crud: Optional[ConceptCRUD] = None,
        task_crud: Optional[TaskCRUD] = None,
    ):
        self.roadmap_crud = roadmap_crud or RoadmapCRUD(RoadmapMetadata)
        self.concept_crud = concept_crud or ConceptCRUD(ConceptMetadata)
        self.task_crud = task_crud or get_task_crud()
    
    async def generate_roadmap(
        self,
        session: AsyncSession,
        request: RoadmapGenerationRequest,
    ) -> dict:
        """生成路线图（重构版）"""
        # 实现与 roadmap_service.py 相同的逻辑
        # 但直接使用 session 和 CRUD
        pass
```

**第二步：逐步迁移**
1. 先在新端点中使用 V2 服务
2. 验证功能正常
3. 逐步替换旧端点
4. 最后删除 `roadmap_service.py`

**推荐**：在完成高优先级任务后再进行此重构。

---

### 11. management_service.py 级联删除逻辑（行269）

**文件**：`backend/app/services/management_service.py`  
**TODO 位置**：Line 269

**现状分析**：
- `delete_roadmap_permanently` 方法可能需要删除更多关联表

**补全方案**：

```python
# TODO: 根据实际需求添加其他表的删除逻辑

# 删除 ExecutionLog
from app.models.database import ExecutionLog
await session.execute(
    delete(ExecutionLog).where(ExecutionLog.task_id == task.task_id)
)

# 删除 ValidationRecord
from app.models.database import ValidationRecord
await session.execute(
    delete(ValidationRecord).where(ValidationRecord.roadmap_id == roadmap_id)
)

# 删除 EditRecord
from app.models.database import EditRecord
await session.execute(
    delete(EditRecord).where(EditRecord.roadmap_id == roadmap_id)
)

# 删除 IntentAnalysisRecord
from app.models.database import IntentAnalysisRecord
await session.execute(
    delete(IntentAnalysisRecord).where(IntentAnalysisRecord.roadmap_id == roadmap_id)
)

# 删除 NoteRecord（如果存在）
# await session.execute(
#     delete(NoteRecord).where(NoteRecord.roadmap_id == roadmap_id)
# )

logger.info(
    "additional_tables_deleted",
    roadmap_id=roadmap_id,
    tables=["ExecutionLog", "ValidationRecord", "EditRecord", "IntentAnalysisRecord"]
)
```

**验证点**：
- 确保所有表都正确导入
- 测试删除操作不会导致外键约束错误

---

### 12. 自动生成前端类型定义

**文件**：`frontend-next/types/generated/models.ts`  
**TODO 位置**：Line 3

**现状分析**：
- 当前前端类型定义手动维护
- 容易与后端 API 不同步

**补全方案**：

**选项 A：使用 openapi-typescript**

```bash
# 在 frontend-next/package.json 添加脚本
{
  "scripts": {
    "generate:types": "openapi-typescript http://localhost:8000/api/v1/openapi.json -o types/generated/api.ts"
  }
}

# 安装依赖
npm install -D openapi-typescript
```

**选项 B：使用 orval**

```bash
# 安装 orval
npm install -D orval

# 创建 orval.config.ts
module.exports = {
  backend: {
    input: 'http://localhost:8000/api/v1/openapi.json',
    output: {
      mode: 'single',
      target: 'types/generated/api.ts',
      client: 'fetch',
    },
  },
};

# package.json 添加脚本
{
  "scripts": {
    "generate:types": "orval"
  }
}
```

**选项 C：使用 FastAPI 内置的 TypeScript 生成**

```bash
# 在 backend 添加脚本
# backend/scripts/generate_frontend_types.py

from app.main import app
import json

# 导出 OpenAPI schema
with open("openapi.json", "w") as f:
    json.dump(app.openapi(), f, indent=2)

# 使用 datamodel-code-generator 生成 TypeScript
os.system(
    "datamodel-code-generator "
    "--input openapi.json "
    "--output ../frontend-next/types/generated/api.ts "
    "--input-file-type openapi "
    "--output-model-type typescript"
)
```

**推荐**：选项 A（openapi-typescript）最简单。

---

## 📊 执行计划

### Week 1（本周）
- [ ] TODO 6：管理员权限检查（30分钟）
- [ ] TODO 7：计算 in_progress 任务数（30分钟）
- [ ] TODO 8：统计各状态任务数量（30分钟）
- [ ] TODO 1：删除 generation.py 重试端点或重定向（1小时）

### Week 2
- [ ] TODO 4：实现资源修改逻辑（4小时）
- [ ] TODO 5：实现测验修改逻辑（4小时）
- [ ] TODO 2：实现概念重新生成逻辑（2小时）
- [ ] TODO 3：完善 retry_service_new 重新生成准备（2小时）

### Week 3（可选）
- [ ] TODO 9：邮件通知（根据产品需求决定）
- [ ] TODO 11：级联删除逻辑（2小时）
- [ ] TODO 12：自动生成前端类型（4小时）

### Phase 2（未来版本）
- [ ] TODO 10：架构重构（1-2周）

---

## ✅ 验收标准

每个 TODO 完成后需确保：
1. **功能测试通过**：端点返回正确的数据
2. **无 Linter 错误**：代码符合项目规范
3. **日志完整**：关键步骤有 structlog 记录
4. **错误处理**：异常情况有对应的 HTTPException
5. **文档更新**：Swagger 文档正确显示

---

## 📝 注意事项

1. **MVP 原则**：优先完成核心功能，非必需功能可延后
2. **测试先行**：关键功能需编写单元测试
3. **代码复用**：参考已有实现（如 `modify_tutorial`）
4. **数据库事务**：确保操作原子性
5. **错误日志**：便于生产环境排查问题

---

## 🔗 相关文档

- [API 重构完成总结](20260107_API重构完成总结.md)
- [企业级架构指南](../FastAPI_Enterprise_Architecture_Guide.md)
- [Swagger 文档优化指南](../backend/docs/20260107_Swagger文档优化指南.md)

