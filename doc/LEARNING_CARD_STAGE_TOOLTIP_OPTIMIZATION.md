# Learning Card Stage Tooltip 优化

## 概述

优化 My Learning Journeys 和 Featured Roadmaps 卡片的背面显示，改为展示 Stage 信息，并为所有 Stage 项添加 Tooltip 悬浮提示功能，以显示完整的 Stage 名称和描述。

## 实施日期

2025-12-22

## 背景

### 原始设计问题

1. **My Learning Journeys 卡片**:
   - 正面已经显示了进度条和进度百分比
   - 背面再次显示进度信息，造成信息冗余
   - 缺少 Stage 结构化信息的展示

2. **Featured Roadmaps 卡片**:
   - Stage 信息可能被文本截断（line-clamp）
   - 用户无法查看完整的 Stage 名称和描述
   - 缺少交互反馈

### 优化目标

1. ✅ 移除 My Learning Journeys 背面的冗余进度信息
2. ✅ 为 My Learning Journeys 背面添加 Stage 列表展示
3. ✅ 为两个卡片的 Stage 项添加 Tooltip 悬浮提示
4. ✅ 在 Tooltip 中显示完整的 Stage 名称和描述

## 技术实现

### 1. Tooltip 组件集成

使用 Radix UI 的 Tooltip 组件，提供优雅的悬浮提示体验。

**组件来源**: `@/components/ui/tooltip`

**核心 API**:
```tsx
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from '@/components/ui/tooltip';
```

**特性**:
- 延迟显示: `delayDuration={300}` (300ms)
- 动画效果: fade-in/out + zoom-in/out
- 定位系统: 自动根据可用空间调整位置
- 高层级: `z-[100]` 确保显示在翻转卡片之上

### 2. MyLearningCard 组件优化

#### 2.1 Props 接口更新

添加 `stages` 参数支持：

```typescript
export interface MyLearningCardProps {
  // ... 其他 props
  stages?: Array<{
    name: string;
    description?: string;
    order: number;
  }>;
}
```

#### 2.2 背面内容重构

**移除内容**:
- ❌ 进度信息卡片（Progress 区域）
- ❌ 统计信息网格（Completed/Total）
- ❌ 最后访问时间（移至底部）

**新增内容**:
- ✅ Stage 列表展示（最多显示 5 个）
- ✅ 每个 Stage 项的 Tooltip
- ✅ 底部统计信息（概念数 + 预估小时数）

**布局结构**:
```
┌─────────────────────────────────┐
│ 标题 (line-clamp-2)      [菜单] │
├─────────────────────────────────┤
│                                 │
│  [Stages 列表 - 可滚动]        │
│   1. Stage Name               │
│      Description...           │
│   2. Stage Name               │
│      Description...           │
│   ...                          │
│                                 │
├─────────────────────────────────┤
│ 📚 50  ⏱ 25h      Continue → │
└─────────────────────────────────┘
```

#### 2.3 状态处理

**生成中状态** (`generating`):
- 显示蓝色状态卡片
- 显示当前生成步骤
- 底部按钮文案: "View Status"

**失败状态** (`failed`):
- 显示红色状态卡片
- 显示失败原因
- 底部按钮文案: "View Status"

**正常状态**:
- 显示 Stages 列表（如果有数据）
- 如果没有 Stages，显示默认描述
- 底部按钮文案: "Continue"

#### 2.4 Tooltip 实现

```tsx
<Tooltip key={index} delayDuration={300}>
  <TooltipTrigger asChild>
    <div className="flex items-start gap-2 p-2 rounded-lg bg-sage-50/50 hover:bg-sage-100/50 transition-colors cursor-pointer">
      {/* Stage 内容 */}
    </div>
  </TooltipTrigger>
  <TooltipContent 
    side="top" 
    className="max-w-xs z-[100]"
    sideOffset={5}
  >
    <div className="space-y-1">
      <p className="font-semibold text-sm">{stage.name}</p>
      {stage.description && (
        <p className="text-xs text-muted-foreground">{stage.description}</p>
      )}
    </div>
  </TooltipContent>
</Tooltip>
```

**关键配置**:
- `delayDuration={300}`: 延迟 300ms 显示，避免误触
- `side="top"`: 优先在上方显示
- `sideOffset={5}`: 与触发器保持 5px 间距
- `max-w-xs`: 最大宽度限制，保证可读性
- `z-[100]`: 高层级，确保在翻转卡片之上

### 3. FeaturedRoadmapCard 组件优化

#### 3.1 Tooltip 集成

为 Stages 列表添加相同的 Tooltip 功能，保持交互一致性。

**修改位置**: `CardBack` 组件

**实现细节**: 与 MyLearningCard 完全一致

### 4. Home Page 数据传递

#### 4.1 数据映射更新

```typescript
const allRoadmaps = history
  .filter(item => item.status !== 'generating')
  .map((item) => ({
    // ... 其他字段
    stages: (item as any).stages || null, // 新增
  }));
```

#### 4.2 Props 传递

```tsx
<MyLearningCard
  // ... 其他 props
  stages={roadmap.stages}
/>
```

## 文件修改记录

### 1. 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `frontend-next/components/roadmap/my-learning-card.tsx` | 1. 添加 Tooltip 导入<br>2. 更新 Props 接口<br>3. 重构背面内容<br>4. 添加 Tooltip 功能<br>5. 调整底部布局 |
| `frontend-next/components/roadmap/featured-roadmap-card.tsx` | 1. 添加 Tooltip 导入<br>2. 为 Stages 添加 Tooltip |
| `frontend-next/app/(app)/home/page.tsx` | 1. 数据映射添加 stages<br>2. 传递 stages 给组件 |

### 2. 新增依赖

无新增外部依赖，使用项目已有的 `@/components/ui/tooltip` 组件。

## 设计细节

### 1. Tooltip 样式规范

#### 1.1 触发区域
- **背景**: `bg-sage-50/50`
- **Hover**: `bg-sage-100/50`
- **过渡**: `transition-colors`
- **光标**: `cursor-pointer`

#### 1.2 Tooltip 内容
- **背景**: `bg-popover`
- **边框**: `border`
- **阴影**: `shadow-md`
- **圆角**: `rounded-md`
- **内边距**: `px-3 py-1.5`
- **最大宽度**: `max-w-xs` (320px)

#### 1.3 文本样式
- **标题**: `font-semibold text-sm`
- **描述**: `text-xs text-muted-foreground`
- **间距**: `space-y-1`

### 2. Stage 列表规范

#### 2.1 显示规则
- 最多显示 5 个 Stage
- 超过 5 个显示 "+X more stages" 提示
- 列表可垂直滚动 (`overflow-y-auto`)

#### 2.2 Stage 项结构
```
┌─────────────────────────────────┐
│ [1] Stage Name                  │
│     Stage Description...        │
└─────────────────────────────────┘
```

**组件结构**:
- 序号标识: 圆形 badge (`w-6 h-6`)
- 标题: 单行截断 (`line-clamp-1`)
- 描述: 两行截断 (`line-clamp-2`)

### 3. 底部统计信息

**布局**: `justify-between` (左右分布)

**左侧统计**:
- 概念数: `📚 {totalConcepts}`
- 预估时长: `⏱ {totalHours}h`

**右侧操作**:
- 按钮文案: "Continue" / "View Status"
- 样式: sage 色系，带箭头图标

## 交互体验优化

### 1. 悬浮延迟

- **延迟时间**: 300ms
- **目的**: 避免用户快速移动鼠标时频繁触发

### 2. 动画效果

**入场动画**:
- fade-in (透明度渐变)
- zoom-in-95 (缩放从 95% 到 100%)
- slide-in (根据位置方向滑入)

**出场动画**:
- fade-out (透明度渐变)
- zoom-out-95 (缩放从 100% 到 95%)

### 3. 事件处理

- **阻止冒泡**: Tooltip 触发器使用 `asChild`，避免影响卡片点击
- **平滑过渡**: 所有交互使用 `transition-colors`

## 响应式设计

### 1. Tooltip 定位

Radix UI 自动处理：
- 优先在上方显示 (`side="top"`)
- 空间不足时自动调整到其他方向
- 保持在视口内可见

### 2. 滚动容器

Stage 列表在卡片内部可滚动：
- 固定高度: 由卡片整体高度决定
- 滚动条样式: 使用浏览器默认（或自定义）
- 移动端优化: 支持触摸滚动

## 数据来源

### 1. MyLearningCard

**数据路径**:
```
API: getUserRoadmaps(userId)
  ↓
Response: RoadmapHistoryItem[]
  ↓
Zustand Store: roadmap-store
  ↓
Home Page: allRoadmaps
  ↓
Component: MyLearningCard
```

**Stages 字段**:
```typescript
stages?: Array<{
  name: string;
  description?: string | null;
  order: number;
}>
```

### 2. FeaturedRoadmapCard

**数据路径**:
```
API: getFeaturedRoadmaps(limit, offset)
  ↓
Response: RoadmapHistoryItem[]
  ↓
Home Page: featuredRoadmaps
  ↓
Component: FeaturedRoadmapCard
```

## 兼容性处理

### 1. 缺少 Stages 数据

**处理方式**: 显示默认描述文本

```tsx
{hasStages ? (
  <div>/* Stages 列表 */</div>
) : (
  <div className="flex-1 flex items-center justify-center">
    <p className="text-sm text-muted-foreground text-center">
      A comprehensive learning path to master your skills step by step.
    </p>
  </div>
)}
```

### 2. 缺少 Description

**处理方式**: 只显示 Stage 名称

```tsx
{stage.description && (
  <div className="text-xs text-muted-foreground line-clamp-2 mt-0.5">
    {stage.description}
  </div>
)}
```

### 3. TooltipProvider

每个 `CardBack` 组件独立包裹 `TooltipProvider`，确保 Tooltip 功能正常工作。

## 优势总结

### 1. 信息架构优化

- ✅ **消除冗余**: 移除重复的进度显示
- ✅ **信息层级**: 正面概览，背面详情
- ✅ **结构化展示**: Stage 列表清晰展示学习路径

### 2. 用户体验提升

- ✅ **完整信息**: Tooltip 显示完整内容
- ✅ **视觉反馈**: Hover 效果明确
- ✅ **交互流畅**: 延迟和动画自然

### 3. 设计一致性

- ✅ **统一交互**: 两个卡片使用相同的 Tooltip 模式
- ✅ **视觉统一**: 保持 sage 色系和圆角设计
- ✅ **代码复用**: 共享 Tooltip 组件和样式

### 4. 可维护性

- ✅ **类型安全**: 完整的 TypeScript 类型定义
- ✅ **组件化**: 清晰的组件拆分
- ✅ **可扩展**: 易于添加更多交互功能

## 测试建议

### 1. 功能测试

- [ ] Tooltip 在鼠标悬停时正确显示
- [ ] Tooltip 延迟 300ms 后显示
- [ ] Tooltip 在鼠标移出后正确隐藏
- [ ] 完整的 Stage 名称和描述正确显示
- [ ] Stages 超过 5 个时显示 "+X more" 提示
- [ ] 没有 Stages 数据时显示默认文本

### 2. 交互测试

- [ ] 点击 Stage 项不影响卡片翻转
- [ ] Tooltip 不阻止卡片点击导航
- [ ] 滚动 Stages 列表流畅
- [ ] 背面的删除菜单正常工作

### 3. 视觉测试

- [ ] Tooltip 样式符合设计规范
- [ ] Stage 项的 hover 效果正确
- [ ] 背面布局在不同内容长度下正常
- [ ] 动画效果流畅自然

### 4. 边界测试

- [ ] Stage 名称/描述为空时的显示
- [ ] Stage 数量为 0 时的显示
- [ ] 超长 Stage 名称的换行和截断
- [ ] Tooltip 在卡片边缘的定位

## 后续优化建议

### 1. 性能优化

- [ ] 考虑虚拟滚动（如果 Stages 数量很多）
- [ ] Tooltip 内容懒加载
- [ ] 使用 `React.memo` 优化 Stage 项渲染

### 2. 功能扩展

- [ ] 点击 Stage 项跳转到对应模块
- [ ] 添加 Stage 完成状态标记
- [ ] 支持拖拽调整 Stage 顺序（管理界面）

### 3. 无障碍性

- [ ] 添加 ARIA 标签
- [ ] 键盘导航支持
- [ ] 屏幕阅读器优化

### 4. 移动端优化

- [ ] 触摸长按显示 Tooltip
- [ ] 优化 Tooltip 在小屏幕上的显示
- [ ] 改进滚动体验

## 相关文档

- [My Learning Journeys 3D 翻转实现](./MY_LEARNING_JOURNEYS_3D_FLIP_IMPLEMENTATION.md)
- [Featured Roadmaps 实现](./COVER_IMAGE_OPTIMIZATION.md)
- [Tooltip 组件文档](../frontend-next/components/ui/tooltip.tsx)

## 总结

本次优化成功改进了 My Learning Journeys 和 Featured Roadmaps 卡片的信息展示：

1. **信息优化**: 移除冗余的进度信息，改为展示结构化的 Stage 列表
2. **交互增强**: 为所有 Stage 项添加 Tooltip，显示完整信息
3. **体验统一**: 两个卡片模块使用一致的交互模式
4. **代码质量**: 类型安全，无 Lint 错误，易于维护

**实施状态**: ✅ 已完成  
**测试状态**: ⏳ 待测试  
**部署状态**: ⏳ 待部署

